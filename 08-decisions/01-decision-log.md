# Decision log

| Date | Decision | Rationale | Status |
|---|---|---|---|
| 2026-07 | Position Hold around temporary loss of communication capacity, not ADHD alone | Broader shared problem and avoids diagnostic exclusion | Active |
| 2026-07 | Use “Create a Hold” as the primary action | Distinctive, calm and product-ownable | Test |
| 2026-07 | Include a return journey in MVP | Relationship repair is central, not an add-on | Active |
| 2026-07 | No streaks or overdue states | Contradicts low-guilt philosophy | Active |
| 2026-07 | AI drafts but never sends autonomously | Preserves consent and relationship agency | Active |
| 2026-07 | Use native share sheet for early MVP | Realistic integration path and lower technical risk | Active |
| 2026-07 | Test £2.99–£3.99 monthly and £19.99–£24.99 yearly | MVP is intentionally narrow; lower entry price better matches early trust and feature depth | Hypothesis |
| 2026-07 | Do not launch with weekly or lifetime pricing | Avoid pressure and unknown long-term cost exposure | Active |
| 2026-07 | Rename "Create a Hold" → Going Quiet, "Return from Hold" → Reconnect; add Taking Time and Conversations as distinct stages | Four emotionally distinct stages serve different practical/emotional purposes and shouldn't be collapsed | Active |
| 2026-07 | Home screen has three states: Normal, Taking Time, Post-Reconnect | Reflects actual user state rather than resetting too early; Post-Reconnect avoids losing unfinished personalised replies | Active |
| 2026-07 | Reconnect persists the audience from Going Quiet by default (add/remove allowed, no full re-pick screen) | Reopening "who should know" at Reconnect reopens an already-settled, emotionally loaded decision and adds friction at low capacity | Active |
| 2026-07 | Reconnect offers "Quick Connect" vs "Personalise"; Personalise leads directly into Conversations rather than a separate drafting screen | Removes a redundant intermediate step; Conversations already does the personalised-reply job | Active |
| 2026-07 | Conversations is a standalone destination, not only reachable after Going Quiet | Broadens usefulness — someone can get help with one hard reply without needing to have gone quiet first | Active |
| 2026-07 | "Send an update" (Taking Time) and "Add to Going Quiet" (new contact while away) added as optional, low-key actions | Handles long quiet periods and new incoming contact without creating pressure or a permanent nagging button | Active |
| 2026-07 | Circle setup happens inline during Going Quiet, not separate onboarding; full management lives in Settings | Avoids making setup feel like labour before the user can do the one thing they came to do | Active |
| 2026-07 | Main interaction circle stays a simple filled geometric shape; no open/closed, scribble or "hug" enclosing shape | Earlier hug/circle-being-held direction read as too soft/gendered; state communicated through scale, weight and colour instead | Active |
| 2026-07 | Motion principle: "Action pages are responsive, state pages are restful"; Going Quiet settles inward, Taking Time breathes subtly, Reconnect opens outward (sunrise) | Keeps interaction snappy while giving the emotional transition room on the following screen | Active |
| 2026-07 | Taking Time uses a warmer, golden-hour palette across all screens in that state; Reconnect fades back to the fresh palette | Reinforces state environmentally rather than through text alone | Active |
| 2026-07 | Circle-pill selection uses a dark green border (not fill change) to show selection, with Close Circle given a stronger fill and priority position | Fixes an earlier bug where the picker's visual selection state didn't match the actual recipient shown at review | Partially superseded 2026-08-11 (below) — the dark-green selection border and Close's priority position stand; Close's stronger default fill is removed, replaced by a bolder font weight as its only remaining secondary cue |
| 2026-07 | Revise pricing to a Founding Member launch offer: £17.99/year or £1.99/month, locked in for life while subscribed; rising to £29.99/year or £2.99/month standard | Rewards early supporters, keeps launch price low-friction, avoids the earlier untested £2.99–£3.99/£19.99–£24.99 range | Superseded 2026-08-11 (below) — Founding Member/Standard split and both monthly cadences removed; £17.99/year is now the only Hold+ subscription price |
| 2026-07 | Add a one-tap "Pause subscription" option | Matches the product's no-guilt philosophy; the people who need Hold most are also likely to be overwhelmed by a difficult cancellation flow | Active |
| 2026-07 | Build Hold natively as an Expo/React Native app, developed with Claude Code (Xcode + VS Code); no-code (FlutterFlow) path explicitly not pursued | Resolves the earlier parallel-exploration platform question; the native build is already running on a physical device with a paid Apple Developer account enrolled | Active |
| 2026-07, response mechanism corrected 2026-08-10 | Safeguarding approach: a detection layer on free-text input before AI drafting, showing crisis resources (Samaritans, Shout, 999, 111 option 2) plus a lightweight safety-plan option (grounding prompt + notify a Core Circle contact) rather than numbers alone. **"Stopping the flow" (this row's original July wording) is superseded, not current** — the 2026-08-10 entry below reversed that to a non-blocking persistent banner; drafting stays available throughout, since the message being written is very often the real reaching-out behaviour Hold exists to enable | Evidence shows numbers alone are rarely used even when repeatedly offered; a co-created safety plan is the most-used intervention in comparable products (Wysa). Hard-blocking drafting was rejected as fighting the app's own purpose — see the 2026-08-10 row's own reasoning | Active — exact trigger logic and wording require legal/clinical sign-off, see `08-decisions/04-open-questions.md`; response *mechanism* (non-blocking, not a hard stop) is settled, see the 2026-08-10 row below |
| 2026-07 | Navigation: Home / Conversations / History as bottom nav, Settings behind a top-right gear icon; Circles set up inline during first Going Quiet, managed thereafter in Settings | Keeps the core journey to one clear action; supporting tools stay accessible without competing for attention | Active |
| 2026-07 | Library (message/email/OOO/social templates) lives nested inside Conversations, not as its own bottom-nav destination | Templates support the reason someone opens Conversations; they aren't a reason to open the app on their own | Active |
| 2026-07 | Drop the ChatGPT-side emoji Circle categories (Core Circle, Friends, Care, Professional, Community); keep the category names as default template categories inside Library, not as fixed Circle names | Emoji don't fit Hold's calm visual language; user Circles stay freeform/user-named, but the category names are useful defaults for organising templates | Active |
| 2026-07 | Settings opens as a right-side slide-out panel (Gmail's drawer pattern, mirrored right), behind a hamburger icon rather than a gear | Keeps context of where the user was rather than a full-screen jump; hamburger tested as the preferred icon over a gear | Active |
| 2026-07 | Bottom navigation uses labelled icons (icon + short word), never icon-only | Research on icon comprehension under cognitive load shows labelled icons require less mental effort than unlabelled ones, especially relevant given who uses Hold and when | Active |
| 2026-07 | Where Circles are shown as avatar-style thumbnails, use a coloured selection ring (Instagram story-ring pattern) rather than a border-only treatment | Avatar-shaped equivalent of the existing dark-green-border selection principle, not a separate pattern | Active |
| 2026-07 | Circle selection uses fully rounded/stadium-shaped pills, not circles or rectangular chips; selected state may use a white glow ring (Instagram story-ring idea) alongside the dark-green-border option | Circles crop or shrink variable-length Circle names; a generously rounded pill keeps most of the calm of a circle while staying legible at any name length | Active |
| 2026-07 | Home tab keeps its label regardless of underlying state, but its icon is Hold's own circle mark, not a literal house glyph | Nav labels name the destination, not the momentary state; the circle mark ties the tab bar back to the app's one core shape instead of an unrelated pictogram | Active |
| 2026-07 | ~~Settings panel splits into Profile (Manage Circles, Notifications, Connected accounts) and About (values blurb + links to Privacy Policy and Terms) rather than one flat list~~ **Superseded, see next row** | — | Superseded |
| 2026-07 | Settings is a single About screen with headed sections (Mission & values, Manage Circles, Notifications, Connected accounts, Privacy Policy, Terms), not a Profile/About split; bottom nav stays at three tabs (Home, Conversations, History) — About/Account does not become a fourth tab | One screen with headed sections gets the same chunking without an extra decision fork; About/Account is occasional-use, same category as Circles, which were already kept off the bottom nav for the same reason | Active |
| 2026-07 | Hold+ gets its own small, persistent, quiet element in the top bar, separate from the About/hamburger icon | Reachable in one tap rather than requiring the Settings drawer to be opened first; may be styled slightly more visibly just after signup, then settles into a permanently quiet presence — visible, never pushed | Active |
| 2026-07 | Add the North Star principle ("every screen works with one tap, but rewards users who have the energy to do more") as the lead entry in `01-foundation/03-principles.md` | This was referenced repeatedly across earlier ChatGPT-side design work but had never actually been written into the principles file itself | Active |
| 2026-07 | Reconnect audience controls include an explicit "expand to full Circle" bulk option, alongside individual add/remove | Covers the case where the user wants to reconnect more broadly than the persisted Going Quiet subset, without losing the default of a narrower, deliberate list | Active |
| 2026-07 | Add a plain-language privacy summary line to `06-privacy-security/01-privacy-by-design.md`: Hold doesn't read WhatsApp, texts or emails, with the Email out-of-office toggle as the one explicit, opt-in exception | Makes the existing detailed privacy principles legible as a single plain sentence a user (or a reviewer) can quickly confirm | Active |
| 2026-07 | The Taking Time reconnect nudge interval is a user-chosen preference, not a fixed system default | Matches "invitation, not reminder" framing; a one-size interval imposed on everyone would sit awkwardly next to the rest of Hold's user-controlled pacing | Active |
| 2026-07 | Added a distinct "Going Quiet again, mid-Conversations" edge case: a small Going Quiet entry stays on the Conversations screen, skips Home, auto-advances (with Skip) straight to "Who needs to know?"; old unfinished Conversations stay saved rather than clearing | Keeps momentum for someone at very low capacity who might otherwise abandon the flow if forced back through Home first | Active |
| 2026-07 | Within a selected Circle at Going Quiet, the user can untick individual people or split out one or two for a personalised message while the rest get the default | Granularity should be per-person, not locked to sending the same message to an entire Circle at once | Active |
| 2026-07 | Patterns records timing/duration/frequency data and optional user-added notes (free text or quick tags like PMDD, migraine, flare, burnout); explicitly never measures message count, conversation length, or content — only that reconnecting started and the quiet period completed | Hard privacy boundary: Patterns is about the user's time away, not an analysis of their conversations | Active |
| 2026-07 | Added recurring language conventions: consistent use of "your circles," "keep your circles in the loop" as the recurring product phrase, and avoiding copy that implies the user has or should have a large support network | Keeps terminology and framing consistent across onboarding, marketing and in-app copy; a small Close Circle should never read as inadequate | Active |
| 2026-07 | Resolved: bottom nav stays at three tabs, Circles/Patterns do not become separate tabs — but the middle tab is renamed from Conversations to **Library**. The screen still leads with the Conversations reply-help experience first, Templates second | A persistent nav tab called "Conversations" can read as a standing to-do list by convention alone, even with no badges; "Library" avoids that while the screen's actual content preserves discoverability of the help function | Active |
| 2026-07 | ~~Resolved: Reconnect stays a branching choice (Quick Connect vs Personalise, Personalise routes to Conversations), not a linear two-stage flow — but Quick Connect gains inline light editing without leaving the page~~ **Superseded, see below** | — | Superseded |
| 2026-07 | Confirmed "Patterns" over "Insights" as the naming for the History-based observation feature | "Insights" is generic SaaS language; "Patterns" is a word people already use about their own health, and reads as self-recognition rather than the app analysing/judging them | Active |
| 2026-07 | History displays previous quiet periods as one card per period (dates, duration, who was notified, reconnected status); Patterns reached via a "History \| Patterns" segmented control at the top of History, not a separate bottom-nav tab | Patterns is occasional-use, same category as Circles/About which were already kept off the bottom nav; segmented control keeps it one tap from where people naturally go to review past periods | Active |
| 2026-07 | Resolved: Patterns exists at a basic level on Free (quiet period count, average duration, simple monthly timeline, days Taking Time) with Hold+ adding richer analysis (seasonal trends, recurring timing, changes over time, optional health-note correlations, longer-term summaries) — not gated entirely behind Hold+ | Matches Hold's broader philosophy that the core experience is available to everyone and the paid tier adds depth rather than withholding something fundamental; also resolves the earlier "locked tab" paywall framing question — it's "upgrade for more," not "unlock this" | Active |
| 2026-07 | Created `03-product/05-roadmap.md` (referenced in the README but never actually written): MVP → v1.5 Hold+ launch → v2 Communication Insights/Pattern Recognition → v3 (Messaging Channels, Health integration, exports) → Future (wearables, carers, family — explicitly conditional on still fitting the philosophy) | The roadmap reference existed in the repo map with nothing behind it; this was a real gap, not just missing detail | Active |
| 2026-07 | Resolved the vague "Customisable reminders" Hold+ line: basic notification control (including the Taking Time nudge interval) is free for everyone; only finer-grained control (per-Circle timing, multiple reminder types) would be Hold+ | "No essential safety or accessibility feature should be paywalled" applies to wellbeing-adjacent reminders; the basic version shouldn't be gated | Active |
| 2026-07 | Added AI personalisation (writing-style learning, saved-messages-as-context, relationship-aware drafting) as a named Hold+ cluster, flagged as needing its own privacy review before being scoped, since it means storing and reprocessing the user's own past content | More specific than "richer pattern recognition"; using someone's own messages as AI context isn't automatically fine just because it's their own data — needs the same purpose/minimisation treatment as anything else in `06-privacy-security/01-privacy-by-design.md` | Active — not yet fully scoped |
| 2026-07 | Free/paid split explicitly driven by a cost model: near-zero-cost features (Circles, History, basic Patterns, Library storage, SMS/share-sheet sending, Email OOO) stay generous on Free; AI-assisted drafting is the one real, usage-scaling cost and is the primary metered lever, as one shared monthly pool across all AI-assist surfaces rather than per-screen quotas | Aligns "genuinely useful free tier" with "app pays for its own AI cost" without arbitrary feature paywalls; episodic (not daily) usage keeps average per-user AI cost low even with a generous cap | Active — exact allowance number and Patterns computation cost (cache vs. per-view AI generation) still need real usage data, see `08-decisions/04-open-questions.md` |
| 2026-07 | Added "Time since last quiet period" to the Free-tier basic Patterns data points | Found in a review of parallel ChatGPT-side material; a simple, near-zero-cost data point consistent with everything else already free | Active |
| 2026-07 | Clarified sync scope: covers Hold's own data (Circles, Library, History, Patterns, settings) across a user's own devices only — never email/health app connections, which each device authenticates to independently | Prevents scope confusion between "Hold syncs my data" and "Hold links my other accounts together," which are very different privacy/engineering commitments | Active |
| 2026-07 | Scoped the vague "family or caring-circle" roadmap idea into a narrower Trusted Contact concept: opt-in by the user only, status-only visibility, silently revocable, always a specific named person, never automatic | The general idea carries a real coercion/misuse risk (a controlling partner or parent using it to monitor someone); needs its own consent and threat-model review before any build work | Active — concept only, not built or fully reviewed |
| 2026-07 | Proposed a one-time, non-subscription purchase for a single GP/clinician export, not yet reflected in the subscription pricing model | Captures revenue at a free user's highest-motivation moment (an upcoming medical appointment) without forcing a subscription decision for an occasional need | Proposed — not yet decided whether it sits alongside or replaces anything in the subscription model |
| 2026-07 | Reweighted the free/paid split: Circles moved from a 3-Circle Free cap to unlimited-and-free, stated as a trust point; the one-time export purchase elevated to a first-class part of the monetisation strategy, not a minor add-on; Trusted Contact removed from the paid feature list entirely and kept deliberately unmonetised even if built | The 3-Circle cap wasn't tight enough to drive upgrades or generous enough to be a trust differentiator — it wasn't earning its keep either way; monetising Trusted Contact risks Hold appearing to profit from something adjacent to surveillance, which isn't worth the trade-off for the revenue it would add | Active |
| 2026-07 | AI implementation approach: connect to an existing model via API (Claude), no fine-tuning; separate system prompts per journey surface; Haiku-tier model for safeguarding detection, Sonnet-tier for drafting; verified actual per-draft cost (well under £0.01 on Sonnet-tier pricing, July 2026 rates) | Confirms the generous free AI allowance is genuinely cheap to offer, not just optimistically assumed; fine-tuning isn't worth the cost/complexity before real usage data exists | Active |
| 2026-07 | Corrected the Settings panel structure: not a single About screen with headed sub-sections — the hamburger opens a panel where About, Hold+, Manage Circles, Notifications, Connected Accounts, Privacy Policy and Terms are each their own row/screen, About being one item among several rather than the container for the rest | Matches the intended structure more precisely; Hold+ appearing as both a panel row and a persistent top-bar element are two separate, coexisting access points, not a contradiction | Active |
| 2026-07 | Resolved the stale account/auth section in `04-ux-content/05-onboarding-empty-states.md` (was still showing "not yet decided" after the model had already been settled elsewhere): no account required ever for the core app; local encrypted storage handles Circles/templates/History/Patterns regardless; account only matters for optional backup/sync, offered later in Settings, not onboarding. Renamed "Account deletion" requirement to "Data deletion" to match | Local storage answers "can I save things" independently of the account question; onboarding stays completely account-free; the previous requirement name implied an account that doesn't exist by default | Active |
| 2026-07 | Finalised the Reconnected screen copy (was direction-only): "You're reconnected." / "You've let the people who matter know you're here again. That's enough for today." / button "Done" | Completes the third of three transition screens with full copy, matching the quiet-acknowledgment tone of the other two rather than leaving it as a placeholder direction | Active |
| 2026-07 | Resolved first launch: single welcome screen (final copy written), deliberately diagnosis-agnostic rather than naming a specific condition; confirmed no account/backup screen in onboarding, reaffirming the prior decision rather than reversing it | Matches the diagnosis-agnostic positioning already established; avoids solving a data-loss problem before the person has any data to lose | Active |
| 2026-07 | Added Feedback and Share rows to the Settings panel; moved Privacy Policy and Terms to the bottom, visually separated from the daily-use items above them | Legal reference items shouldn't sit at the same visual priority as things people might actually tap | Active |
| 2026-07 | Restructured Reconnect from a branching choice screen (Quick Connect vs Personalise) into a single symmetric tap, matching Going Quiet — Reconnect now leads straight into a transition then Conversations, where the Quick message vs Personalise choice happens per person instead | Home's two primary actions (Going Quiet, Reconnect) should both be single, calm taps; the actual decision-making belongs where it's already needed — per person, inside Conversations, which tracks per-person state anyway | Active — supersedes the earlier branching-choice decision |
| 2026-07 | "We" (team voice) reserved for the welcome screen, Our Mission content, and Feedback — not used inside the core journey (Going Quiet, Taking Time, Reconnect, Conversations, transition/completion screens), which refer to "Hold" in the third person instead if the product needs naming | Core journey copy should centre the user and their circles, not Hold-the-company; "we" belongs to genuine team-to-user moments only | Active |
| 2026-07 | Resolved the "Send an update" visibility timing (hidden the calendar day Going Quiet is sent, visible from the next day) and the reconnect-nudge default (on, not opt-in, but delivered only as a soft in-app prompt, never a push notification) | Predictable calendar-day logic beats session-based triggers; opt-in-only nudges mostly reach people who already knew to look, undercutting the point; delivery method is what actually protects against frustration, not the default | Active — exact interval remains an untested hypothesis, not evidence-based |
| 2026-07 | Renamed the "About" Settings row to "Our Mission"; added a "Research" row surfacing the evidence base already documented in `02-research/` (accessibility research, safeguarding evidence, icon/label findings) | Clearer label than the generic "About"; the research already exists as internal documentation and costs nothing new to expose honestly as a trust-building page | Active |
| 2026-07 | Added the recipient-benefit line to the welcome screen copy: "It helps the people who care about you too — so they're not left wondering if they've done something wrong, or worrying in silence" | Ties the welcome message to the "second, more subtle cost" already established in `01-foundation/02-problem-and-opportunity.md` — Hold helps both sides of a relationship, not just the sender | Active |
| 2026-07 | Restored a priority "Send to everyone" bulk quick-message action at the top of Conversations, with a confirm step to prevent accidental sends | The per-person restructure accidentally removed the original single-tap point of Quick Connect — sending the same message to a whole list shouldn't require repeating a per-person action once per contact | Active |
| 2026-07 | Extended Conversations into three tiers: Send to everyone (Tier 1) → per-Circle bulk send with a different message per Circle allowed (Tier 2, new) → per person, Quick message or Personalise (Tier 3). List grouped by the Circles originally messaged at Going Quiet, not a flat list. New contacts added via "+ Add person" one at a time, not in bulk | Mirrors the Circle-grouped structure Going Quiet already uses rather than treating Conversations as an unstructured list; a middle granularity (per-Circle) was missing between "everyone" and "one person" | Active |
| 2026-07 | Reconnect's gate prompt ("Want to reply to anyone properly?") appears only after Send, not after Personalise; tapping Personalise always goes straight into Conversations with no gate (build-time) | The book's own wording contradicted itself — a gate after both Send and Personalise doesn't square with "Personalise is entering Conversations, one door not two," since choosing Personalise already answers the gate's question | Active |
| 2026-07 | Deleted the old full-page Personalise screen and its standalone "Replies in progress" queue entirely, rather than leaving the queue screen in place, once Personalise became an inline accordion inside Conversations (build-time) | The queue screen only existed to route into the full-page editor; once that's gone it had nowhere to send "Open," so keeping it would have shipped broken links. Its standalone role is intentionally deferred to Library (Section 3), not lost | Active |
| 2026-07 | Split export into two tiers: raw data export (CSV/text, free for everyone, always) and the formatted GP/clinician PDF report (paid, one-time, now £3.99, down from the earlier £5.99–6.99 range) | Charging for someone's own raw data sits uncomfortably against data portability norms; once the paid version is clearly the formatting/analysis service rather than data access, a lower price fits what's actually being sold | Superseded 2026-08-11 (below) — the free/paid export split stands; the paid tier is renamed Patterns Report and reprised at £2.99 |
| 2026-07 | Reaffirmed £29.99/year standard Hold+ pricing (post-Founding Member) as reasonable, not reduced | Unlimited AI and multi-month Patterns trend detection are genuine computational value-adds, not access being withheld from something that should be free — still materially cheaper than comparable wellbeing subscriptions researched earlier | Superseded 2026-08-11 (below) — the £29.99/year Standard price no longer exists; £17.99/year is the only Hold+ subscription price. The underlying reasoning (unlimited AI and Patterns trend detection are genuine value-adds, not withheld access) still applies to justifying £17.99/year, just not to a second, higher tier |
| 2026-07 | Finalised the welcome screen copy after on-device review (small tightening, "We built Hold" instead of "We built this," dropped "so they're not left wondering if they've done something wrong"); added "no em dashes in drafted copy" as a formal voice rule | Confirms the copy on a real device rather than just in the book; the no-em-dash rule was an existing personal preference that should have been applied to the original draft | Active |
| 2026-07 | Further tightened the welcome screen: split "We'd love to hear what you think" into its own sentence; removed "by letting them know you need some space" as redundant with "keep your circles in the loop" already stated in the first paragraph | Shorter sentences match the pacing of every other screen in the app; the second paragraph should state the benefit to recipients, not repeat the mechanism already established | Active |
| 2026-07 | Removed an em dash that had slipped into the Reconnect transition copy ("Welcome back. Here's who's waiting to hear from you. Reply however feels right today."), found while confirming the transition screens for Claude Code | Missed when the no-em-dash rule was added; caught on a routine check rather than left for later | Active |
| 2026-07 | Added the missing circle text hierarchy spec (Going quiet/Reconnect as bold headers, subtitles beneath) to the actual book — this decision existed informally but was never written into `04-ux-content/01-core-journeys.md`, which is why the first build implemented Taking Time as a single undifferentiated line | The book is the source of truth Claude Code reads; an undocumented decision is effectively an undecided one from the build's perspective | Active |
| 2026-07 | Resolved: Personalise expands inline within Conversations (matching Quick message's existing inline pattern), not a separate screen navigated away to | Preserves the ability to keep working through other people's Quick messages without losing place; keeps the "per person, inside Conversations" restructure principle intact | Active |
| 2026-07 | Added a concrete type scale to `05-design-system/02-colour-and-typography.md` after on-device review found text and input boxes too large/clunky: only screen titles and primary buttons stay large, everything else (body copy, message boxes, option labels, secondary buttons) drops to a compact scale comparable to Instagram/Gmail | The existing typography guidance was qualitative only ("clear at small sizes," "generous line height") and evidently wasn't specific enough to prevent an oversized first build | Active |
| 2026-07 | Fully specified inline Personalise as an accordion panel (one open at a time, trimmed fields, compact chip-style starting points, status collapses back to the row on close) rather than leaving "make it inline" as an underspecified instruction | Several fields from the original full-screen version (name, four stacked option cards, retention control) needed real decisions about what carries over and how, not just a location change | Active |
| 2026-07 | Clarified the type-scale fix must shrink container padding and spacing proportionally with the text, not font size alone | Small text left inside unchanged, oversized card padding would produce a mismatched result, not a fixed one | Active |
| 2026-07 | Circle picker moves from wrapped multi-row pills to a single horizontal scrollable row; "+ New Circle" placed as a fixed icon-button outside the scroll, not first or last inside it; visible content should cut off partially at the row's edge as a scroll cue | Scales cleanly now Circles are unlimited on Free; resolves the tension between Close Circle needing to stay first/prominent and "+" needing to stay discoverable without scrolling | Active |
| 2026-07 | Refined "+ New Circle" placement further: a round icon button (matching Hold's circle motif) at the right end of the "Who needs to know?" heading line, not beside the pill row | Keeps the scrollable row purely about Circle selection; treats "+" as a heading-level action, a more familiar pattern than stranding it beside a horizontal scroll | Active — supersedes the immediately prior placement decision |
| 2026-07 | Resolved the compact-chip context trade-off: full explanations of what each Going Quiet template communicates live in Library, not in the live composition flow, where chips stay deliberately compact and unexplained | Keeps the fast path fast for someone who already knows what a template means; the explanation is still available, just somewhere looked up occasionally rather than repeated every time | Active |
| 2026-07 | Going Quiet template chips only appear the first time messaging a given Circle; the choice becomes that Circle's saved default afterward, editable in Library, with a small "Change template" link kept as an occasional escape hatch | Recurring quiet periods shouldn't require re-deciding message style every time; first-time setup keeps the helpful scaffolding, repeat use becomes Circle-plus-Send | Active |
| 2026-07 | Conversations Tier 2 redesigned: Circle name as a pill header with recipient names listed inline, collapsed by default (message box, bulk send, dropdown arrow only) rather than the full per-person list always open; dropdown expands into Tier 3 | Reduces the vertical space both tiers took up when always shown together; matches the broader compactness direction from the type-scale fix | Superseded — see next row |
| 2026-07 | Made the per-Circle default-saving mechanism explicit: a stated "Save to Library" action with explanatory text on first use, and the same Save affordance available on every subsequent visit — editing and tapping Save updates the default, editing without saving applies to that message only | Makes a previously silent background behaviour transparent; prevents an occasional one-off tweak from accidentally overwriting someone's usual default | Active |
| 2026-07 | Corrected Tier 2: removed inline recipient names from the pill header. Users generally already know their own Circle's membership; anyone who wants to check can expand the existing dropdown into Tier 3, which already shows the full per-person list, or check Manage Circles in Settings | The dropdown already answered the "who's in this" need without also needing names cluttering the collapsed view; Going Quiet's own Circle picker never showed member names either, so this keeps both screens consistent | Active |
| 2026-07 | Added a down-arrow expand affordance to selected Circle pills in Going Quiet, opening a recipient box (tick/untick, personalise for one or two) beneath the picker row — the same interaction as Conversations' Tier 2 → Tier 3 dropdown, only one Circle's box open at a time | The per-person capability already existed in the spec but had no clear visual trigger; unifying it with Conversations' existing pattern means one consistent expand-a-Circle mechanism across both screens instead of two different ones | Active |
| 2026-07 | Resolved the previously-unspecified post-send state in Conversations: sending replaces the Quick message/Personalise links with a status label, Personalise stays available afterward even after a Quick message was sent, and the completion tick is a separate, deliberate action never auto-triggered by sending | Someone may want to follow up with a fuller reply after a quick one, matching the existing Post-Reconnect "finish later" home state; "Conversation complete" already covers completion paths beyond Hold sending something, so it should stay the user's own judgement | Active |
| 2026-07 | Rejected full colour inversion for the Tier 2 sent-state pill (would collide with the existing white/outlined = secondary navigation meaning); adopted desaturate-and-soften with a paired checkmark instead | Colour inversion reuses a treatment already assigned elsewhere, creating ambiguity; desaturating extends the existing "settling" colour language and keeps the accessibility rule that state must never rely on colour alone | Active |
| 2026-07 | ~~Major restructure of Conversations: replaced the always-visible three-tier layout with progressive reveal — a Circle selection row plus one shared message box by default; per-Circle boxes only appear once "All" breaks; per-person boxes only appear once someone's unticked~~ **Superseded, see next row** | — | Superseded |
| 2026-07 | Split Conversations into two dropdowns: Quick message (top — the progressive-reveal All/per-Circle flow from the previous decision) and Personalise/Conversations (second — per-person AI-assisted). Unticking someone from a Quick message group moves them to the second dropdown entirely, rather than gaining a nested individual instant-message box within Quick message. Sent-state pill loses its selection halo when it desaturates. Status label reads "Instant message sent" / "Personalised reply sent" immediately, becomes the actual date from the next calendar day onward | Splitting by purpose (bulk/low-effort vs individual/proper-attention) is cleaner than nesting a third tier inside the bulk flow; a frozen "sent" label goes ambiguous the longer Taking Time continues, a date doesn't; a sent pill showing both the old selection ring and the new sent treatment would look like two states layered together | Active — supersedes the single-section progressive-reveal version |
| 2026-07 | Added a research-backed governing voice principle applied app-wide (gentle short validating statements; permission without pressure or commentary; never praise a basic act of communication; never narrate the person's psychology back to them), backed by a new research file `02-research/06-guilt-spiral-and-supportive-language.md` and surfaced in the in-app Research page | Rejected the "well done, this helps your guilt spiral" direction for status copy; lived-experience research consistently shows brief validation and permission help, while praise and psychological commentary add pressure | Active |
| 2026-07 | Reconnect screen has exactly three actions (Send, Edit, Personalise); Conversations does not appear until an instant message is sent or Personalise is tapped, then a "Want to reply to anyone properly? / Not now" gate appears | Showing Personalise and a visible Conversations section at once is redundant (two doors to the same place); hiding Conversations until triggered protects the calm default view | Active |
| 2026-07 | Finalised Reconnect status/gate copy in the new voice: instant sent = "Sent. They know you're thinking of them." (date from next day), personalised sent = "Sent. They'll hear from you properly.", gate = "Want to reply to anyone properly? / Not now"; template save confirmation = "Saved" with a one-time "You can edit this anytime in Library" on first save only | One consistent voice across the whole Reconnect flow, mirroring Going Quiet's warm-framing-to-quiet-permission arc | Active |
| 2026-07 | When more than one Circle is selected at Going Quiet Step 1, each Circle gets its own separate message box rather than one shared box for the whole send (build-time) | A single shared box would silently apply one Circle's saved default to a different Circle's send, a correctness problem rather than just a simplification; matches Conversations' existing per-Circle rows so both screens share one mental model. A single Circle selected is unaffected, still one box | Active |
| 2026-07 | Corrected the app's template storage from an earlier category-tagged model (`core-circle`/`friends`/etc.) to Circle-keyed saved defaults, matching what the book already specified (build-time) | Category-tagged templates in the code were a misreading, not an intended feature — emoji Circle categories were explicitly rejected earlier for not fitting the calm visual language, leaving only category names as default template groupings. This brings the code back in line with the per-Circle saved-default model already described in `04-ux-content/01-core-journeys.md`, rather than cutting a feature | Active |
| 2026-07 | Resolved the open "Keep this on your device for" retention window as a three-tier model: Library permanent (own words only); "Your reply"/unsaved Going Quiet/Reconnect edits persist 48 hours, surviving app close, to protect an in-progress reply through interruption; a pasted-in message from someone else clears after 4 hours or on the app fully closing, whichever is sooner. Never surfaced as a countdown or warning in the flow; disclosed plainly in the privacy policy, with at most a one-time calm mention on first save | Retention should map to whose content it is and what it costs to lose, not one blanket timer — losing an in-progress reply could stop someone very unwell from reconnecting at all, while another person's pasted message is both more sensitive and cheaper to lose, so it should clear sooner | Active |
| 2026-07 | Corrected Reconnect's gate: "Not now" returns to Home (which resolves to its own Post-Reconnect state, "Finish Reconnecting" available) rather than jumping straight to the Reconnected screen. Supersedes the prior "'Not now' skips straight to the Reconnected screen" wording, which contradicted the journey overview's own "Reconnected, once Conversations is complete" (build-time correction, confirmed) | The instant message alone doesn't mark anyone in Conversations complete, so forcing Reconnected here would declare the journey done on the user's behalf while people are still genuinely unaddressed; Reconnected should only ever fire from Conversations/Library's own completion check, matching what the rest of the book already said | Active |
| 2026-07 | Added a Templates section to Library, beneath Conversations: every Circle with a saved Going Quiet default, editable in place — satisfying the existing "can also be changed any time in Library directly" requirement (Going Quiet item 2), which had no built home until now. Scoped to the one template type with real data behind it today (a single saved default per Circle); the richer multi-type/category vision stays deferred, per the existing "Library internal organisation" open question, not cancelled. Also fixed: `saveCircleTemplate` had no guard against saving empty text, so Library's own Save action is disabled whenever a row's box is empty or whitespace-only, preventing a permanent blank row (build-time) | The requirement already existed with no built home; scope stays honest to what has real data today rather than inventing template types/categories with nothing behind them yet. The empty-save gap was a real correctness risk once editing happens through a new UI surface, unlike when Going Quiet's own flow was the only writer | Active |
| 2026-07 | New capability: Quick message's sent state is now reopenable. A fully-sent Circle's pill (or, when "All" is fully sent, the shared "Sent to everyone" summary in its place) can be tapped to reopen the same message box and Send action for a follow-up send, rather than staying permanently locked with no way to message that Circle or everyone again for the rest of the Conversations session. Reuses the existing send/confirm/mark-sent mechanism unchanged; a repeat send simply refreshes the status label's timestamp | Previously there was no way to send a genuine follow-up through Quick message once a Circle (or everyone) had been sent to once, which is a real gap for anyone reconnecting over more than one message — resending should be as easy as the first send, not require falling back to Personalise or another channel | Active |
| 2026-07 | Built History's Patterns segment (segmented control: History \| Patterns) with the Free tier in full — quiet-period count, average duration, time since last, total days spent Taking Time, and a single-month calendar reusing History's existing calendar view. Hold+ richer Patterns (multi-month trends, recurring timing, health-note correlations, longer-term summaries) is represented as a single plain-text line naming what it adds, not a locked upsell or a "Coming later" tag, since Patterns itself isn't missing, only the richer tier is. Also deferred: per-period health notes/tags and CSV/PDF export — both real features in their own right (new data field and interaction; tied to unbuilt business/pricing infrastructure), not byproducts of the navigation shell (build-time) | No subscription/entitlement system exists yet, so Hold+-tier Patterns has no real feature behind it to build; representing it honestly as "more exists later" rather than a fake locked button matches how Hold+/Notifications were already handled earlier in Section 3, and the book's own instruction that Patterns should never read as a locked upsell | Active |
| 2026-07 | Built the Settings slide-out drawer (hamburger, right-edge overlay) replacing the old corner "About" icon everywhere. "Our Mission" and "Privacy Policy" split apart the old About screen's mission and privacy content into their own rows/screens; "Research" is a new screen condensing hold-book's six research files. Corrected my own earlier assumption that Connected Accounts was "already mocked and ready" — checking again, there's no persisted account list at all, only ephemeral per-session state inside Going Quiet's email out-of-office step, so it joins Hold+/Notifications/Terms as a "Coming later" row rather than a real screen. "Delete everything on this device," not in the book's panel list at all, gets its own row just above the Privacy Policy/Terms divider, per direct instruction (build-time) | Reusing the app's existing genuine mission/privacy content rather than writing new stub text keeps the new screens as honest as the old About screen was; Connected Accounts turned out to have nothing real to show once checked properly, the same category of gap as Hold+/Notifications | Active |
| 2026-07 | Reordered the Settings panel from the book's original flat Mission-first list into four purpose-grouped sections, task-oriented content first: Manage Hold (Circles, Notifications, Language, Connected Accounts) leads, then About Hold (Our Mission, Research, Hold+), then Support (Feedback, Share), then Legal and data (Privacy Policy, Terms, Delete my data) behind the one divider. "Language" confirmed to have no real functionality anywhere in the app, so it joins Notifications/Connected Accounts/Hold+ as a "Coming later" row rather than a real one. Renamed "Delete everything on this device" to "Delete my data," confirm-then-wipe behaviour unchanged | Someone opening this menu while unwell is usually here to do something practical — adjust a notification, check a Circle — rather than to read about the app's values; task-oriented content should come before browsing content, especially for a low-capacity user. This is a UX ordering decision, not an ambiguity resolution | Active |
| 2026-07 | Set the drawer's group spacing so About Hold → Support is the single largest gap (the one real section break, values/browsing to practical Support), with Manage Hold → About Hold and Support → the Legal-and-data divider sharing the same smaller gap as each other. Considered icons for drawer rows, matching the icon-plus-label reasoning already used for the three bottom-nav tabs, and deliberately deferred them for now — a dozen rows each needing a distinct, legible glyph is a bigger task than three tab icons, and plain text is already clear on its own; not a closed door, worth revisiting once the drawer's contents settle | Recording the icon consideration so the reasoning isn't lost if this gets revisited later, rather than looking like an oversight; the spacing hierarchy needed to be explicit since "groups separated by spacing" alone doesn't specify which gap is biggest | Active |
| 2026-07 | "Delete my data" uses a darker, calmer red derived from the existing error/destructive token, not the same red used at its usual size/weight for Circle-deletion and Hold-history confirms, and not a new one-off colour. Landed at roughly an 85/15 blend toward the body text colour after an initial 70/30 blend read as too dark on-device — closer to brown/grey than red — so the ratio was lightened back up to stay clearly red at a glance | The same red read as more alarming applied to a passive row label sitting among plain-text rows than it does on an active confirm button — this is the one truly irreversible action in the drawer, so the destructive signal needed to stay unambiguously red, just toned down to match the calmer surrounding panel, not muted to the point of losing the signal entirely | Active |
| 2026-07-29 | Settings drawer spacing and destructive-label colour (build-time). Tightened row height (48→44) and inter-group spacing so groups read with clearer contrast: groupSpaced xxl→xl, groupExtraSpaced xxl\*1.5→xxl, groupWithDivider marginTop xxl→lg (Share Hold now sits close to the divider line) and paddingTop lg→md. Shifted "Delete my data" from #7C2D2D to #A6342A, pulling the blend back closer to the raw error hue since the prior value still read as brick-brown rather than red; #B3392E noted as a punchier alternative if #A6342A still reads brown on an actual phone screen | Visual review found within-group and between-group spacing too similar (groups didn't read as distinct), and the destructive-red label was blending too far toward the near-black text colour, reading as brown rather than red | Active |
| 2026-07-29 | Dropped Hold+'s persistent top-bar element entirely, superseding the earlier two-access-point wording in `04-ux-content/04-navigation-architecture.md` (build-time, confirmed by direct instruction) | A standing visual element in the top bar, even styled quietly, reads as ongoing pressure/visual noise, inconsistent with Hold's "held, not managed" tone | Active — supersedes "Hold+ visibility"'s prior top-bar-element wording |
| 2026-07-29 | Built Patterns' Hold+ surfacing as an additive "More with Hold+" card shown below the free stats, tapping through to the Hold+ info screen — not a locked or greyed-out preview (build-time) | A padlocked/greyed chart mockup would read as "you can't see your own data," which is subtly punishing; a plain, calm description of what the richer view adds keeps it an invitation | Active |
| 2026-07-29 | Built `app/settings/hold-plus.tsx` as an honest Hold+ info screen — real content from `07-business/02-pricing-principles.md` (free tier, what Hold+ would add, Founding Member pricing table, fair-access commitments), closing with a plain "not open for purchase yet" note, no working Subscribe button. Both the drawer's Hold+ row and Patterns' "More with Hold+" card route here (build-time) | No entitlement/billing system exists in the app, and the book itself never specified a paywall screen to build against — a fake purchase button would be dishonest chrome, and a dead stub would leave both access points pointing nowhere | Active |
| 2026-07-29 | Corrected the £3.99 formatted GP/clinician PDF export from a Hold+ feature-list bullet to its own standalone "One-time purchases, available to everyone" section in `07-business/02-pricing-principles.md` — it's purchasable by free-tier users too, independent of any subscription, not a Hold+ perk | Its own text already said "one-time, non-subscription purchase," but nesting it under the "### Hold+" heading read as gated behind a subscription anyway; the new Hold+ info screen and this document now agree | Superseded 2026-08-11 (below) — the standalone-section placement stands; the product itself is renamed Patterns Report and reprised at £2.99 |
| 2026-07-29 | Dropped the planned 7–14 day free trial for Hold+ — supersedes the "7–14 day trial" and "clear reminder before trial conversion" lines previously under "Ethical access options" in `07-business/02-pricing-principles.md` | The free tier is already generous enough (full journey, unlimited Circles/History/Library, basic Patterns, a shared AI allowance, and the GP/clinician PDF export available free-tier too) that a trial isn't needed to let someone evaluate whether Hold works for them; one-tap cancel/pause and no retention flow still stand and do the same trust-building work a trial would otherwise do | Active — supersedes the prior trial lines |
| 2026-07-29 | Dropped the iOS "Done" keyboard-accessory bar built for every text input app-wide; tap-outside-to-dismiss is the single keyboard-dismiss method on both iOS and Android instead | `InputAccessoryView` doesn't render under React Native's New Architecture (`newArchEnabled: true` in this app's `app.json`) — confirmed on-device after building it correctly. Chose not to add a new native dependency (would require a custom dev client instead of Expo Go) or disable the New Architecture app-wide for one bar | Active |
| 2026-07-29 | Merged Going Quiet into a single scrollable screen — no separate Review step. Send fires immediately as the one essential action (group message, individual instant messages, and starting Taking Time); email out-of-office, wider-world status, and a "want to send anyone something more personal?" re-offer move to an optional unwind after Send, closed by one explicit "Done" tap rather than an automatic trigger | Reduces the screen to one thing to do at a time — the essential notification, done immediately, with nothing else competing for attention before it — mirroring the shape Reconnect already uses (essential action first, optional depth after). An automatic move-on trigger was considered and rejected: an untouched toggle is ambiguous about whether someone decided "not now" or just hadn't got to it, which runs against this app's own low-capacity design principles | Active — supersedes the prior two-step Who-needs-to-know/Review structure |
| 2026-07-29 | Added a second-level selection-circle control per excluded recipient (fully-remove vs keep-with-own-instant-message), visually matching Home's main action circle (filled = included, white + border = excluded) rather than the previous checkbox. Removing someone collapses their row to name-only, muted; the same control restores them exactly as before | Colour must be paired with a shape/fill change, not colour alone, per the existing selection-state contrast rule — a checkbox already satisfied that, but re-using the app's own primary-action circle motif reads as more consistent with the rest of Hold's visual language than a generic checkbox would | Active |
| 2026-07-29 | "Personalise," tapped for an excluded Going Quiet recipient, routes them to Conversations instead of composing inline — but seeding into Conversations' Personalise bucket is deferred until Send actually fires, not done at the moment of tapping (confirmed directly, not assumed, since the prior book/app behaviour was inline composition and no decision-log entry from the cited date existed to check against) | Nothing real exists to show in Conversations before Send commits the Going Quiet round anyway; deferring keeps a still-in-progress Going Quiet screen from prematurely writing Conversations records for a journey the person might still change their mind about | Active — supersedes the prior inline-personalisation description |
| 2026-07-29 | Rewrote Taking Time's "Send an update" to a multi-select "All" + per-Circle chip row (reusing Conversations' Quick-message chip styling) sharing one message box, replacing the earlier single shared-box-for-everyone design. Sent state persists per Circle across re-opening the screen within the same Taking Time period, held in the same flow-state object that already resets at Hold-period boundaries elsewhere, and Send is followed by a brief calm confirmation that auto-dismisses back to Taking Time rather than requiring a tap to close | A reassurance update is a low-friction, repeatable ping — letting someone choose a subset of Circles for one message, and see at a glance who's already been updated this period, fits that better than one audience-wide box with no memory between visits | Active — supersedes the prior single-shared-box design |
| 2026-07-29 | Added an "All" pill to Going Quiet's Circle picker (already multi-select, just missing the shortcut). Confirmed Library's Quick message picker already had one — no change needed there, correcting an assumption that it was missing (build-time) | Consistency across every Circle picker in the app, per direct instruction — auditing what actually exists rather than assuming a gap before building | Active |
| 2026-07-29 | Library: selecting a second "Other"-bucket (no-Circle) individual for a shared message now triggers an inline "Create a Circle for these people?" confirm; accepting reveals a name field on the same screen (no navigation to Manage Circles), creating the Circle pre-populated with the selection via the existing createGroup/addContactToGroup (build-time) | Reuses the existing Circle mechanism rather than a second, parallel "select multiple loose individuals" system; resolves inline rather than navigating away, consistent with every other in-flow decision point built this session | Active |
| 2026-07-29 | Rewrote Reconnect from a single-audience message box into a multi-select "All" + per-Circle + per-ungrouped-individual picker with one shared message box, matching the pattern already used for Taking Time's update screen and Going Quiet's picker | Consistency across the app's Circle pickers, and moves Reconnect off the one screen that still forced an all-or-nothing send to the entire persisted audience at once | Active — supersedes the prior three-action (Send/Edit/Personalise) single-box screen described above |
| 2026-07-29 | Reconnect now gates "Want to reply to anyone properly?" (and newly, turning off out-of-office / clearing wider-world status) behind full coverage — every Circle and ungrouped individual must be reached at least once, across as many separate sends as needed. Removed the previous "remove someone already dealt with outside Hold" allowance, since it's incompatible with a hard coverage requirement — flagged as a real capability loss, not just a rewording, worth revisiting if it turns out to matter | Ensures nobody is silently skipped or forgotten during Reconnect, which is core to what the feature is for; the trade-off (no way to mark someone as handled via another channel) was accepted directly, not assumed | Active — supersedes the prior "remove someone already dealt with" allowance in the Audience section above |
| 2026-07-29 | Reconnect coverage tracking (who's been reached) is stored durably on the Hold period record itself, not just in-memory app state, via a new reconnecting-period marker independent of the existing open-period marker — confirmed directly rather than assumed after an initial draft of this plan wrongly scoped it to in-memory context only. Home gains a fourth state, "Reconnecting," that resumes an interrupted session in place rather than showing a misleading Normal Home | A force-quit mid-Reconnect must never lose track of who's already been reached — the user should never have to remember or reconstruct this themselves, which is directly core to what Hold is for, particularly for the chronic-illness/reduced-capacity users it's built around. Documented in `docs/03-privacy-model.md` as a narrow, justified persistence exception | Active |
| 2026-07-29 | Out-of-office/status "turn off" at Reconnect is mocked and symmetric to the existing mocked activation side (no real email/social provider integration exists for either) — whether either was enabled at Going Quiet is now persisted onto the Hold period record, since neither previously had any persisted "is this on" state anywhere in the app (build-time, confirmed by reading the actual service code rather than assuming reverse logic already existed) | Building real deactivation without real activation would be dishonest scaffolding; the honest move is a symmetric mock plus the minimum persistence needed for Reconnect to know whether to even show the controls | Active |
| 2026-07-30 | Chose Cloudflare Workers to host the AI-drafting proxy, over an always-on server (fixed cost regardless of traffic) and over Vercel/Lambda (more setup complexity or product mismatch for a solo builder with no existing backend) | Workers' free tier comfortably covers MVP volume, cost scales with actual episodic usage rather than sitting fixed, and Workers KV solves the usage-metering need without standing up a second product | Active |
| 2026-07-30 | Built the free-tier monthly draft allowance (Workers KV, keyed by an anonymous per-install UUID) into the very first version of the AI proxy, not as a follow-up once the endpoint was already live | With no user accounts, the bundled app-to-proxy shared key is a weak secret on its own (extractable from the app binary) — the rate limit and monthly cap are what actually bound the cost/abuse risk, so they had to exist before the endpoint was reachable at all, not after | Active |
| 2026-07-30 | Built the actual server-side AI-drafting integration (worker/hold-ai-proxy, real Claude API calls with per-surface system prompts from this file) — draftService.ts's createDraft/createReplyDraft now try it first and fall back to the existing local templates on any failure, never removing the non-AI route | Went through draftService.ts's own pre-existing gate (privacy review, retention rules, content minimisation, disclosure, safe failure, non-AI route) directly with the product owner before building, rather than either skipping it or building a fake stub indefinitely | Active |
| 2026-07-30 | Reconciled a real internal contradiction between decisions 89 (chip pick auto-becomes the saved default) and 91 (an explicit "Save to Library" tap is required) — both now hold at once: the pristine, unedited chip pick auto-persists as the Circle's default with no Save tap needed, while an edit away from it only carries forward via an explicit Save; an unsaved edit reverts to the saved default on the next visit rather than silently persisting (build-time correction to `04-ux-content/01-core-journeys.md`, which had never actually stated this) | The code before this fix implemented neither version cleanly — an unsaved edit was resurfacing on the next visit via a per-Circle draft-autosave mechanism never intended to carry across separate Going Quiet occurrences | Active — supersedes the contradiction, not either individual prior row |
| 2026-07-30 | Fixed a real bug: `Screen.tsx`'s `ScrollView` set only `contentContainerStyle`, never a `style` prop, so it wasn't bounded to the viewport — it sized to its own content like a plain View, working fine until content exceeded one screen's height (several Going Quiet Circle cards), at which point the extra content had no way to be reached (build-time) | Found via code reading, not guessed — the fix (`style={{flex:1}}` on the ScrollView itself) is a shared-component fix, so it applies to every screen using `Screen`, not just Going Quiet | Active |
| 2026-07-30 | Added a single-contact-Circle exception to the second-level exclusion control in `04-ux-content/01-core-journeys.md`: a Circle with only one contact never shows the fully-remove toggle, since removing the only person already equals not selecting the Circle at all | Confirmed this wasn't covered by the existing "Circles reconciliation" scope (that item's own description was limited to verifying against Section 2's per-person opt-out work) before building, rather than assuming it was already handled | Active |
| 2026-07-30 | Added per-recipient/Circle send-channel metadata to Quiet History (SMS compose vs. the generic share sheet, plus which app when iOS reports one) — process metadata only, never message content, surfaced as a plain "Sent via ..." line per History card. Scoped to the two flows History already tracks (Going Quiet, Reconnect); Taking Time updates and Library/Conversations sends aren't part of the Hold-period record today | Same category as the timestamps/recipient names Quiet History already stores, per direct instruction; documented in `docs/03-privacy-model.md`'s existing Quiet History section rather than as a new exception | Active |
| 2026-07-30 | Sizing audit against `05-design-system/02-colour-and-typography.md`'s existing compact type scale found four genuine drifted spots, all in work built after the original type-scale pass (decision 83): Going Quiet's per-Circle section label, Home's "Taking time" state label, History's Patterns stat-tile values, and Taking Time update's confirmation text — all brought back to ~17pt. Confirmed as non-violations: screen titles, PrimaryButton labels, Home's circle label/wordmark (branding, not body copy), and icon-glyph chevrons (month-nav, drawer row) | The type-scale spec itself needed no changes — only enforcement had drifted in newer components, exactly as anticipated before auditing | Active |
| 2026-07-30 | Confirmed Hold+ placeholder-flag mechanism directly before building it (per explicit "confirm before wiring anything to it" instruction): lives on the existing Hold+ info screen as a clearly-labelled "Simulate Hold+ active" dev/test switch, persisted via SecureStore (survives restarts across a test session) through a dedicated `holdPlusService.ts` — every future consumer gates through `isHoldPlusActive()` so real entitlement logic later is a one-file swap | Avoided guessing a UI location/storage mechanism for a placeholder that everything else (Amend with AI, AI memory) would gate on | Active |
| 2026-07-30 | Built "Amend with AI" (Hold+): a light-touch blend, not a regenerate, in the same position (below the box, above Send) across Going Quiet, Reconnect, Taking Time's update, and Personalise/Conversations. Worker gets a new request shape (`existingMessage`/`additionalContext`) and an amend-specific system-prompt instruction to edit only what's necessary and keep the rest. Personalise's "What they sent" box additionally settles into a muted, Edit-able card once populated, pinned above the reply box, never collapsing | See `04-ux-content/01-core-journeys.md`, "Amend with AI," for the full UX shape, and `06-privacy-security/02-ai-boundaries.md` for the request/prompt mechanics | Active — requires a `worker/` redeploy to take effect against the live proxy |
| 2026-07-30 | Built AI memory (Hold+): Layer 1 standing "Remember helpful details" toggle (off by default, Hold+ area, never in-the-moment), Layer 2 quiet capture via the same Amend-with-AI request (model returns an optional short note, parsed server-side) and later surfacing as a "Use it"/"Don't remember" suggestion on Taking Time and Reconnect only — never Going Quiet. Documented in `docs/03-privacy-model.md` before any code was written, per the existing convention | Retention was initially specified to match "the existing Hold+ trial expiry window," but no such trial exists anywhere in the app — flagged rather than silently resolved. Confirmed directly afterward: 90 days stands as its own decision regardless, long enough to cover a real illness cycle end-to-end, short enough to avoid indefinite retention of personal details | Active |
| 2026-07-30 | Added a fixed, illustrative before/after example of Amend with AI to the Hold+ info screen's existing "What Hold+ would add" content, explicitly labelled as a static example rather than a live/interactive preview | Per direct instruction, matching the screen's existing honest-content-only convention (decision 115) rather than building a working interactive demo with nothing real behind it yet | Active |
| 2026-07-31 | iOS 26 gives custom native-header views (headerLeft/headerRight, wrapped internally in a UIBarButtonItem) a "Liquid Glass" shared pill background by default, regardless of the component's own transparent styling — not visible/fixable at the React Native style layer at all. Fixed via react-native-screens 4.22's `unstable_headerLeftItems`/`unstable_headerRightItems` API with `hidesSharedBackground: true`, kept alongside the existing `headerLeft`/`headerRight` as the Android fallback since the items API is iOS-only (build-time) | Found via reading react-native-screens' native iOS source (`RNSBarButtonItem.mm`, `RNSScreenStackHeaderSubview.mm`) after style-level fixes repeatedly failed to make the Back button/hamburger read as flush | Active |
| 2026-07-31 | Circles reconciliation: Going Quiet's "+ New Circle" moves from a heading-level icon button (the book's prior spec, never actually built that way) to pinned inside the Circle-picker row, right after "All" — "All" and "+ New Circle" fixed in place, only the named-Circle pills scroll. Confirmed directly, not assumed, after finding the book's spec, the actual shipped code, and a recent direct instruction all described three different layouts | The heading-icon pattern moved to Your Circles' header instead (it has its own title/header bar Going Quiet's inline step doesn't); resolving which of three conflicting descriptions was correct needed a direct answer, not a guess | Active — supersedes the heading-icon-button placement in `04-ux-content/04-navigation-architecture.md`'s Circle picker layout section |
| 2026-07-31 | Circles reconciliation: confirmed the contacts-picker is circles' sole add-contact path — no separate manual name/phone-number entry form, correcting `03-mvp-requirements.md`'s "manually or through permission-based selection" wording, which described a manual path that was never built | The contacts picker alone already covers the practical need; a second parallel entry mechanism wasn't worth the added surface area | Active — supersedes the "manually" half of the prior Hold Circle requirement wording |
| 2026-07-31 | Circles reconciliation: confirmed Library's category-based starter templates (Core Circle/Friends/Care/Professional/Community headings) are an unbuilt roadmap item, not a gap to close now — per-Circle saved template defaults already cover the practical need | Avoids scoping new work for something the existing, simpler mechanism already solves; noted directly in `04-ux-content/04-navigation-architecture.md` so it reads as a deliberate deferral, not a missed requirement | Active |
| 2026-07-31 | Settings drawer's Circle-management row corrected to read "Your Circles" in `04-ux-content/04-navigation-architecture.md`, matching the screen's actual shipped title/header (moved into the header bar earlier this batch) rather than the book's stale "Manage Circles" wording (build-time) | The row a user taps and the screen it lands on should agree; the app's title was the deliberately, recently confirmed one | Active — supersedes "Manage Circles" wherever named in the drawer panel order |
| 2026-08-10 | Data/account reconciliation: audited every persisted key against "Delete my data" and found three real gaps — the AI-memory Layer-1 toggle stayed on after a wipe (only the notes it captured were cleared), the Hold+ "simulate active" dev flag survived, and (confirmed directly, not assumed, given the free-tier AI allowance no longer exists) the AI proxy's anonymous per-install rate-limit id survived too. All three now clear as part of "Delete my data" (build-time fix; the install-id inclusion itself was a direct confirmation, not a build-time guess, once the free-tier context made the earlier keep-it-stable rationale moot) | A "delete everything" action leaving any persisted state behind, even non-content state, undermines the plain-language promise the button makes | Active |
| 2026-08-10 | Confirmed "Delete my data" stays content-only — the one-time seen-welcome-screen and seen-retention-note flags are deliberately left alone, not cleared, so a wipe doesn't force re-onboarding. Corrected `04-ux-content/04-navigation-architecture.md`'s item 12 to state this explicitly, and removed the app's actual `AsyncStorage.removeItem(HAS_SEEN_WELCOME_KEY)` call, which had drifted out of sync with this principle (it cleared the welcome flag but not the retention-note flag, an inconsistency with itself as well as with this decision) | Checked whether skipping onboarding post-wipe leaves the user on a screen that wrongly assumes existing data first — it doesn't: a wiped-but-onboarded Home resolves to the same empty Normal state every genuinely new user sees before their first Circle exists, so there's no broken edge case to design around | Active — supersedes the app's prior (undocumented, inconsistent) welcome-flag-clearing behavior |
| 2026-08-10 (retroactive — decision predates this entry, first formally logged here) | Free-tier AI-assisted drafting removed entirely, for cost reasons — AI drafting is now Hold+-only, with no free allowance of any size. Superseded a live code gap found and fixed this session (`docs/09-decision-log.md` in `hold-app`, "AI-assisted drafting is Hold+-only"): `draftService.ts` had no Hold+ gate at all, and the Worker's `FREE_MONTHLY_DRAFT_LIMIT` was still a real, reachable 20/month free allowance, not just stale wording. Also superseded: the 2026-07 "generous free AI allowance" entries above (AI implementation approach; the free-trial-drop reasoning citing "a shared AI allowance") and the free-tier bullet in `07-business/02-pricing-principles.md`'s Free tier list, all corrected this session | Not recorded here at the time it was actually decided — this entry exists so the book stops silently disagreeing with what's actually shipped, not to relitigate the reasoning | Active — supersedes every prior "free AI allowance" framing in this log and in `07-business/02-pricing-principles.md` / `06-privacy-security/02-ai-boundaries.md` |
| 2026-08-10 | Flagged, not resolved: `07-business/06-business-strategy.md`'s ARR/conversion reasoning still assumes the pre-removal free-AI-tier framing (a generous free tier including AI was part of what the conversion-rate argument leaned on) and needs a manual review pass against the decision above — explicitly left untouched this session, since it's a real strategic re-think rather than a factual correction | Sales/strategy narrative shouldn't be mechanically edited to match a code change; the person who owns the strategy should decide how the argument changes, not have it rewritten under a "sync the book" pass | Not yet decided |
| 2026-08-10 | Safeguarding detection: decided a two-tier architecture — free tier gets on-device keyword/phrase matching only (no network call, zero marginal cost); Hold+ tier gets the same layer plus an additional classifier pass (Haiku-tier, via the existing server-side proxy, ~£0.0004–0.0005/check, planning estimate). Tiering applies only to detection accuracy — the response once something triggers (resources, wording, flow) is identical regardless of tier, so free users are never left with a lesser response. Also decided: checked surfaces are Going Quiet's own-words box, Personalise's "Your reply," and Amend with AI's prompt box (all the user's own words); "What they sent" (someone else's pasted message) is explicitly out of scope for this layer, tracked as its own future open question instead. Also decided: the triggered response is a non-blocking persistent banner above the message box (resources + Core Circle notify), not a hard stop — drafting stays available, since the message being written is very often the real reaching-out behaviour Hold exists to enable (see the Wysa 2.4%-called-a-helpline evidence already in `06-privacy-security/03-safeguarding.md`) | A paid-only safety net would be a real harm risk for free users, so baseline protection had to be universal with accuracy (not protection itself) as the only Hold+ differentiator; hard-blocking drafting on trigger was rejected as fighting the app's own purpose, and is flagged for the clinical/legal reviewer as a real safety judgment call rather than assumed-safe UX | Active |
| 2026-08-10 | Safeguarding detection: build the full pipeline now (detection function, banner UI, resource content, grounding prompt, Core Circle notify) rather than wait for legal/clinical review to start any of it — but the actual detection content (keyword/phrase list, classifier criteria) ships only as an explicitly-marked placeholder, hard-gated to never run outside local dev builds, never reaching TestFlight, beta testers, or production | Ordinary app plumbing (the pipeline itself) doesn't need to wait for anyone's sign-off, but the safety-critical content genuinely does — mirrors how Hold+ itself is built as a real, working, clearly-labelled placeholder rather than either faked or left entirely unbuilt | Active — real trigger logic and wording remain on hold pending clinical safety consultant input |
| 2026-08-10 | Rewrote `07-business/06-business-strategy.md`'s "Revenue realism check" to reflect the free-tier AI removal's effect on the conversion argument — previously flagged as needing a manual review pass (2026-08-10 entry above), now done directly by the person who owns the strategy. Reframed from one clear signal (free tier being generous lowers conversion pressure) to two forces pulling in different directions: AI drafting being fully paywalled now presents a harder wall (toward higher conversion), but a free tier with no AI is less compelling to use or recommend day-to-day (against higher conversion). Which dominates isn't resolvable from reasoning alone — stays flagged as depending on real usage data this project doesn't have yet, with the honest expectation still conservative-to-modest rather than assuming the harder wall alone raises it | The reasoning needed to actually change, not just have its now-inaccurate premise deleted — a real re-think, done directly rather than mechanically edited to match the code change | Active — supersedes the prior single-direction "lower conversion" framing |
| 2026-08-10 | Circle naming/grouping/shape, five changes together: (1) renamed the pinned default Circle from "Close Circle" to "Close" — a real name-string rename (`circleService.ts`'s `CLOSE_CIRCLE_NAME`), with a one-time migration for already-persisted installs, since the name was never user-editable; (2) confirmed no separate fix was needed to "drop 'Circle' from pill labels" — no call site ever appended a "Circle" suffix, so this was already true and only appeared to need fixing because of (1); (3) added "Friends" as a suggested-Circle chip, ordered first, ahead of "Work"/"Book Club"; (4) added a "Core" section heading (with explanatory copy) above Close and any Circle literally named "Friends" in Your Circles, purely organisational/display, not merging their data/membership/templates — other Circles keep rendering in the existing flat, unheaded list; (5) Circle shape changed from pill-always to true-circle-by-default, falling back to the stadium pill only when the label's actual rendered text (measured live against the user's current font scale, not a character-count guess) doesn't fit inside the hard-minimum accessible circle diameter (44pt iOS / 48dp Android) — isolated into one new shared `AdaptiveCircleChip` component, which replaced and deleted the now-redundant `CirclePill` | Renaming needed a migration path or already-onboarded users would be stuck on the old string forever, since the name was never editable through the UI; the Core heading and Friends addition reflect how relationships actually cluster, not an arbitrary reorganisation; the shape change restores Hold's actual circle motif for names it fits rather than abandoning it for every Circle regardless of name length | Active — supersedes the prior "Decided: pills" rule in `04-ux-content/04-navigation-architecture.md` (kept struck through there for the record) |
| 2026-08-10 | Follow-up fixes to the Circle naming/grouping/shape work above, four together: (1) found and fixed a real bug — the first `AdaptiveCircleChip` implementation never actually rendered a true circle for anything, including single characters, because its invisible measuring `<Text>` (absolute-positioned, measured via `onLayout`) picked up ambient sizing from its containing flex row instead of shrinking to its own content, so measured widths always came back too large; rewritten to measure via `onTextLayout` on the real, always-rendered text, which reports the glyph run's own dimensions directly (build-time, confirmed by code inspection, not visually); (2) the hard-minimum diameter is now a floor (`Math.max` against the measured line height), not a fixed size, so very large accessibility text sizes grow the container instead of clipping it, while never dropping below 44pt (iOS) / 48dp (Android) at any size — confirmed directly from the constants, not by eye, since neither figure can visually verify an on-device render (flagged, not resolved: the actual on-screen appearance still needs checking on-device); (3) removed the old, unbuilt "Core Circle" Library-template-category concept from `04-ux-content/04-navigation-architecture.md` — a different, older idea colliding in name with the new "Core" section heading, confirmed for removal rather than disambiguation; (4) added a small, muted "Where this comes from" link beneath Your Circles' Core section copy, and a matching new Research entry citing Dunbar's layered support-clique/sympathy-group model as the basis for grouping Close and Friends together | A shape rule that never actually produces the shape it's meant to isn't a cosmetic miss, it's the feature not working — worth a real root-cause fix, not a parameter tweak; the minimum-as-floor correction matters specifically because "never smaller than" was being read as "always exactly," which breaks at extreme accessibility sizes; a stated but uncited research claim in user-facing copy needed a real source, not just a plausible-sounding line | Active |
| 2026-08-10 | The shape fix logged immediately above did not actually work — on-device screenshots showed every chip, including "Close" and "All," still rendering as a pill. Root cause was a second, separate bug from the measurement one already fixed: the circle's diameter was floored at 44pt but never allowed to grow past it, leaving only ~24pt of inner width after padding — "Close" alone needs roughly 35–40pt at normal text size, so no real word could ever pass regardless of measurement accuracy. Fixed by letting the circle grow to fit the label (still floored at 44pt/48dp, now capped at a new 1.5x-of-the-floor maximum so it can't grow into an oversized blob), with worked numbers confirming "Close"/"All"/single characters now clear the circle threshold while multi-word names like "Book Club" still don't. Also: GroupPicker's "+ New Circle" button is now just "+" (accessibilityLabel="New Circle" added), position unchanged since it already sat right after "All"; bumped to a 44pt minimum tap target now that there's no label text to pad it out | The prior entry's "confirmed directly from the constants" claim was true as far as it went (the floor value itself was correct) but incomplete — it didn't catch that the ceiling side of the logic made the floor's own branch unreachable. Recorded plainly rather than quietly amending the earlier entry, since decision-log entries are a record of what was actually believed and reported at the time, not a place to retroactively look right | Active — supersedes the sizing-correctness claim in the entry immediately above; still flagged, not resolved: no on-device screenshot confirms this pass either, only code/constant inspection |
| 2026-08-10 | Circle-chip sizing redesigned again, per direct instruction: replaced the whole floor/cap/grow-to-fit-per-label model with one fixed `STANDARD_CHIP_DIAMETER` (48pt iOS / 52dp Android, platform-specific by design, each a flat +4 above that platform's own accessibility minimum) that every chip in a row shares exactly — a circle at that fixed diameter if the label fits, otherwise a pill at the same fixed height with only width growing, never a per-label height difference. Also found "+" (New Circle) was never actually routed through the shared `AdaptiveCircleChip` component at all — it was still hand-styled separately (the same button manually patched to a 44pt minimum in the entry above), which is exactly why it hadn't picked up any of the shape-component's fixes automatically. Brought it in properly via new `outline`/`accessibilityLabel`/`expanded` props on the shared component, rather than patching its dimensions by hand again | The previous grow-to-fit design, even fully bug-fixed, still produced a row where "D" sat at 44pt and "Close" sat at ~57-60pt — inconsistent, not cohesive, which was the actual complaint underneath the two prior bug reports; a hand-styled "+" outside the shared component would silently drift out of sync with every future sizing change the same way it already had once | Active — supersedes the grow-to-fit/floor/cap model in the two entries directly above and in `04-ux-content/04-navigation-architecture.md`'s Circle-shape section |
| 2026-08-10 | Reverted STANDARD_CHIP_DIAMETER from 48pt/52dp (the "+4 comfortable margin" chosen two entries above) to 44pt/48pt — the bare platform accessibility floor itself — after checking git history: the original pill component this whole redesign superseded was 38pt for its entire life, below the floor, so the floor is the closest legally-compliant match, not a value comfortably above it. Restored a pill's own horizontal padding to that original's generosity, decoupled from the (still tight) padding budget the circle-fit check uses, since the two had been wrongly sharing one constant. Added uniform press feedback directly to the shared AdaptiveCircleChip component (fixing "+"'s dropped press effect from two entries above, properly this time — in the component, not patched per chip). Replaced the single `selected` boolean with two independent flags, `isSelected`/`hasSentThisSession`, with sent-state visually and behaviourally correct by construction (deselecting never clears the sent flag, only a genuine send does). Full codebase audit (grep for the pill shape, cross-checked against grep for the existing chip-state class names) found three genuine Circle-chip sites still outside the shared component; migrated `return/reconnect.tsx` this pass (its own state already correctly implemented the two-flag model, durably, so this was UI-only), left `return/update.tsx` and `(tabs)/library.tsx`'s Quick-message row for a follow-up pending three flagged persistence-scope questions the user hadn't yet answered | Checking git history rather than guessing at "what did it used to look like" turned a subjective complaint into a concrete, defensible number; a hand-styled chip outside the shared component is exactly how "+" silently lost its press effect once already, so the audit was written down in full rather than trusted to memory a second time | Active — supersedes the 48pt/52dp value in the entry immediately above |
| 2026-08-10 | Closed out the Circle-chip migration audit. `return/update.tsx`'s "already sent this Taking Time period" state moved from HoldFlowContext (in-memory, lost on force-quit) to a new HoldPeriod.updateSentCircleIds field, following the same durable-per-flow-store pattern as Reconnect's reconnectContactedIds but scoped to the still-open period rather than a separate marker, since Taking Time's update happens before the period closes. `(tabs)/library.tsx`'s Quick-message row confirmed to have the same repeatable "select some, send, come back for more" shape as Reconnect/update.tsx (each selected Circle gets its own independent card and Send action, not a single shared-box bulk send) — its hasSentThisSession is derived from existing durable per-person completion state (ConversationPerson.completed, already set by markQuickSent) rather than a new persisted field, since that signal already existed just not surfaced at the chip level. All Circle-chip sites found in the full audit two entries ago are now on AdaptiveCircleChip | Each flow's durability question had a genuine, different correct answer rather than one shared rule — Reconnect needed a new marker because its period had already closed, Taking Time didn't because its period is still open, and Library needed no new state at all because the signal was already there | Active |
| 2026-08-10 | Ran a build-sanity diagnostic (temporary, deliberately absurd size + colour change to AdaptiveCircleChip) — confirmed visible on-device after force-quit and relaunch, then cleanly reverted (verified via a clean git diff). Rules out a stale build/cache/Metro/wrong-target explanation for earlier fixes not appearing on-device. Separately found, while scoping a requested on-device walkthrough: Going Quiet's own picker (GroupPicker.tsx) never wires hasSentThisSession at all and structurally can't support the "select, send, come back and reselect" pattern the other four screens have — it's a single whole-screen Send, and once it fires the interactive chip row is replaced by static, non-interactive "sent" text rows, not a chip in any state. Also: "+" (New Circle) moved from pinned-inside-the-row (immediately prior decision) to pinned outside the scroll entirely, with "All" becoming the scrollable row's first item instead — checked all five AdaptiveCircleChip call sites first and confirmed only GroupPicker.tsx actually has a "+" chip to reposition | A walkthrough run against the wrong screen would have looked like a regression that wasn't one; reporting that Going Quiet doesn't support the pattern being tested was more useful than silently letting the test proceed on a screen that could never pass it | Active — supersedes the "+ pinned inside the row" placement in the entry immediately above and in 04-ux-content/04-navigation-architecture.md |
| 2026-08-10 | Confirmed the earlier scroll-to-reach-Send-button fix (padding/HoldMark size tuning on the arrival/completion screens) was genuinely still present in the built code, not reverted or lost — ruling that out before looking further. Root cause was the fix's own category, not its absence: fixed numeric padding/sizing can't hold under all conditions (a longer subtitle at large accessibility text sizes can still push a button below the fold), so it was always going to reopen eventually. Replaced it with a structural fix instead: `Screen.tsx` gained an optional `footer` prop rendered fixed below the scrollable content, inside the same safe area, so the primary action is never part of the scrollable region at all — applied to the three short, fixed-content arrival/completion screens (`create/done.tsx`, `return/done.tsx`, `return/transition.tsx`), deliberately not the three longer compose/work screens (Going Quiet, Reconnect, Taking Time's update) where scrolling to reach Send on a long Circle list is expected behaviour, not a bug. Also corrected Home's "Taking time" state label back up from ~17pt (decision 136's own "bring it back to compact" figure) to the same Large-tier size as the Home circle header it sits beside, per direct instruction that a held state deserves to read as prominent, not tucked away — reopening decision 136's figure for this one spot specifically, not the type-scale audit as a whole | Numeric tuning had already failed once for this exact bug, so patching the same number again risked reproducing it under a slightly different condition; a structural guarantee (never part of the scrollable region) closes the whole category rather than one instance of it. The heading-size correction was a direct, explicit instruction, not a re-audit — decision 136's other three corrected spots stand unchanged | Active — supersedes decision 136's ~17pt figure for Home's "Taking time" state label only |
| 2026-08-10 | Reworked Going Quiet's Send from the single whole-screen "fires once, collapses to checkmark" model to per-Circle and repeatable, matching the interactive selected/sent-this-session chip pattern already proven on Taking Time's update, Reconnect, and Library's Quick message (closing the gap flagged in the entry immediately above). A Circle chip row now sits above the per-Circle cards; only selected Circles show an expanded card. Send fires for the currently-selected Circles only, marks them sent (derived from the Hold period's existing `sendChannels` record — no new persisted field needed, the same derived-not-stored approach Library's Quick message already uses), and collapses their cards — but their chip stays tappable, re-selecting (and reopening the card) on tap so a further or different message can go to the same Circle. Deselecting a sent chip without sending falls back to the sent look, not blank, same rule as everywhere else this pattern exists. The Hold period itself is now created once, on the session's first Send, and reused for every subsequent one (previously a fresh, unconditional call on every Send — never reachable before, since Send only ever fired once). The "want to send anyone something more personal?"/OOO/status/Done sequence now appears once anything has been sent (not once everything has), and the personal-note re-offer is scoped to excluded people belonging only to Circles actually sent so far, not the whole audience — a Circle that hasn't been sent yet has no one to personalise about yet. Confirmed: nothing-sent-yet shows only the picker and a disabled Send; a partial send leaves unsent Circles' cards open and sent ones collapsed-but-reselectable, with Done already reachable; a fully-sent state looks the same as partial, just with every chip in the sent/reselectable look — Done was never gated on "every Circle sent," in this model or the one before it | Direct instruction, scoped explicitly to this mechanic only — not the combination-keyed shared-message-per-group behaviour, the Personalise step, or OOO/Wider World placement, which remain as documented and are follow-on pieces of the same broader redesign | Active — supersedes point 4 (and the affected parts of 5 and 7) of `04-ux-content/01-core-journeys.md`'s Going Quiet section, which described the single fires-once model |
| 2026-08-10 | Three fixes to Going Quiet's post-send state, per direct on-device report against the entry immediately above. (1) Sent Circles now transition colour in place within the one Circle-chip row already used for picking the audience, rather than a second chip row added directly beneath it — the earlier entry's design had added a genuinely separate row for send-selection, distinct from the audience-picker row, which is what read as "a checkmark on a new line" rather than an in-place change; merged into one row, where a not-yet-sent Circle's tap keeps its original "add/remove from audience" meaning unchanged, and only a sent Circle's tap meaning changes, to "reselect for another send." (2) "Add [X] to [Circle] permanently?" no longer appears during Going Quiet at all — moved to Reconnect's own "everyone's been reached" gate, alongside "want to reply to anyone properly?", per a standing decision (not previously written down anywhere in the book, corrected now) that structural Circle-permanence questions belong at Reconnect, once capacity has likely improved, not in the moment of going quiet. No new persisted list needed — a pending Circle's own id already carries a recognisable prefix, so Reconnect finds them directly in the period's existing audienceCircles; only a small "already resolved" durable marker was added, so a later, possibly force-quit-interrupted Reconnect visit doesn't re-ask. (3) Removed "Sent. You've communicated to everyone who needs to know" from Going Quiet entirely, as a deliberate design decision, not a bug fix — the per-Circle checkmark + colour transition already satisfies "never rely on colour alone" at the individual level, the line was also no longer accurate once sending became partial/repeatable, and the positive "you've done what's needed" feeling belongs on the Transition screen instead (not built in this pass, deliberately — a separate piece of work). Flagged back, not fixed: with the confirmation line gone, the OOO/status disclosure and Done button are still stacked directly beneath Send/the picker on the same screen, gated behind "at least one Circle sent," exactly as the 2026-07-29 "single scrollable screen, no separate Review step" decision already specified — not a regression, but worth revisiting now that the screen's balance has changed | The two-row design was a genuine, reasoned choice (an audience-removal tap and a resend-reselect tap have different consequences and seemed unsafe to merge), but diverged from what every other screen using this chip pattern does and read as a bug on-device; the merge stays safe because only a Circle's tap meaning changes once sent, never before. The pending-Circle-permanence timing was apparently already decided once, off the record — writing it into the book now closes that gap rather than leaving it as tribal knowledge again | Active — supersedes the two-row description in the entry immediately above; corrects 04-ux-content/01-core-journeys.md's Going Quiet section (points 1 and 4) and Reconnect's section (new "add permanently?" prompt), and fills a real gap that had no prior book entry at all |
| 2026-08-10 | Split the app's action buttons into two deliberately different shapes, per direct instruction, after today's isSelected/hasSentThisSession rework turned several screens' Send into a repeated, per-item action the old full-width pill no longer fit. Audited every Send-style button first (see docs/09-decision-log.md for the full list) and categorised each as repeated/per-item (Going Quiet's per-Circle Send, Going Quiet's personal note, Reconnect, Taking Time's update, Conversations' Quick message ×3, Conversations' Personalise "Send now") or one-time completion (Reconnect's "Done," "Begin Taking Time," "Continue," "Get started," Manage Circles' "Update circle"). Repeated buttons became a new compact, icon-only `CompactSendButton` — standard paper-plane glyph (Ionicons, a new but standard Expo-first-party dependency, not a custom icon), same Strong/primary green fill as before, same 44pt/48pt accessible tap-target floor as the Circle chips regardless of visual compactness, `accessibilityLabel="Send"` (or a more specific per-recipient variant) carrying the meaning an icon-only button has no visible label for. One-time completion buttons (`PrimaryButton`) kept full-width prominence but moved from `theme.radius.md`'s rounded-rectangle to `theme.radius.pill` (matching every other "strong" filled element in the app, closing the one inconsistency that read as dated) and gained real side margins instead of spanning edge-to-edge of their own already-padded container — still the existing Strong/primary green fill + white text token, no new colour invented. "Begin Taking Time" deliberately stays text-only, no icon — a restful completion moment, not an achievement, and an icon there would collide with Circle iconography/the moon-cycle overlay and add action-energy the screen is built not to have; every other one-time completion button follows the same text-only rule | Repeated taps in a sequential flow and a single significant action are different interaction shapes and read oddly sharing one style; reusing the existing green-fill token for both (rather than inventing a second colour) keeps "this is the affirmative action" legible across both shapes. The audit-first requirement mattered here specifically: several buttons look alike in code (SecondaryButton, PrimaryButton) but serve genuinely different interaction patterns depending on the surrounding flow | Active — supersedes the single full-width-pill Send treatment described implicitly throughout 04-ux-content/01-core-journeys.md wherever "Send" is mentioned as a button; the new "Button patterns" section in 05-design-system/02-colour-and-typography.md is now the source of truth for button shape |
| 2026-08-10 | Two corrections to the day's chip/button sizing work, both with final numbers given directly rather than measured (this environment has no pixel-measurement tooling for the reference screenshots, confirmed and flagged rather than fabricating a measured figure). `STANDARD_CHIP_DIAMETER` increased from the bare 44pt/48pt accessibility floor to 64pt (iOS) / 68dp (Android) — a stylistic, "story-circle" scale choice, well past the accessible minimum either way, not an accessibility fix. Recalculated the circle-vs-pill text-fit table at the new size (see docs/09-decision-log.md for the full worked table): "D," "+," "All," "Close," and "Work" now fit as circles (all previously-pill "Close" now clears the larger threshold); "Friends" and "Book Club" stay pills. `CompactSendButton` (yesterday's repeated/per-item Send) stays at the original 44pt/48pt floor unchanged — deliberately smaller than the Circle chips now, not the same number by coincidence. Also restored a visible reach-count on Quick message's per-Circle bulk Send, previously only in its accessibilityLabel — but as plain text beside the icon ("Send (3 people)"), not a numeral-in-a-circle badge (which would visually compete with the Circle chips on the same screen and risk reading as a notification badge rather than a count), and using "people" rather than "Circles": this button's own existing confirmation alert already reads "Reaches N people," confirming the count was always people within one Circle, never a count of Circles, so "Circles" phrasing would have misdescribed what the button does | Sizing decisions that don't need pixel-perfect matching to a reference are legitimately faster resolved by a direct final number than by chasing a measurement this environment can't actually produce; the "people" vs "Circles" wording needed checking against the button's actual existing behaviour (its own alert text) rather than taking the requested phrasing at face value, since implementing factually wrong copy would have been a worse outcome than a brief correction | Active — supersedes the 44pt/48pt `STANDARD_CHIP_DIAMETER` figure and the accessibility-floor framing in the "Circle shape" section of 04-ux-content/04-navigation-architecture.md, and the "icon-only... same floor as the Circle chips" framing in 05-design-system/02-colour-and-typography.md's Button patterns section |
| 2026-08-10 | Unified text entry app-wide onto one shared component: a docked bar anchored directly above the keyboard (react-native-keyboard-controller's KeyboardStickyView, a new dependency — RN's own InputAccessoryView is already confirmed broken under this app's New Architecture setup, per Screen.tsx's own comment), replacing every in-page TextInput. Audited first, as instructed: 19 distinct free-text fields found across Going Quiet, Reconnect, Taking Time's update, Conversations/Library (shared and per-person Quick message, Personalise's reply, template editing), and every "new Circle" naming field. All moved except one, per explicit and later reconfirmed instruction: Personalise's "What they sent" box, which stays in-page and settles read-only once populated — reference content pasted once, not something actively composed, so it needs to stay visible for scroll-back reference while the reply is written separately in the docked bar, not risk being edited by accident. Every other field, including ones initially flagged as maybe-too-small (the email account nickname), was confirmed in scope by direct instruction: no field is too trivial to route through the one shared pattern. New-Circle suggestion chips ("Friends," "Work," "Book Club") lost their "+" prefix. Added a mic to the docked bar using on-device-only speech recognition (expo-speech-recognition, wrapping iOS's SFSpeechRecognizer/Android's SpeechRecognizer with requiresOnDeviceRecognition forced on) — nothing spoken leaves the device, consistent with the app's existing privacy stance, and deliberately not a third-party cloud STT service. Also: Amend with AI (Hold+) moved from a separate collapsible panel below each message box into a trigger built directly into the docked bar itself — explicitly logged as a supersession of the earlier documented panel design, not a silent replacement, and now available on email out-of-office/wider-world status/Library templates, which never had Amend with AI before this pass | The docked-bar pattern makes "where do I type" and "where do I edit AI-amend context" the same single answer everywhere in the app, rather than a per-screen bespoke box; on-device-only dictation was a deliberate, explicit choice matching the app's own privacy commitments rather than defaulting to higher-accuracy cloud recognition. The one exception (pasted-in "what they sent") is a genuine category difference — reference content someone else wrote, not something the user is drafting — not an inconsistency | Active — supersedes every prior description of an in-page TextInput throughout 04-ux-content/01-core-journeys.md, and the standalone Amend-with-AI panel design in that same section's earlier revisions |
| 2026-08-11 | Logged the current pricing decision, per direct instruction, in `07-business/02-pricing-principles.md`: Hold+ reaffirmed at £17.99/year with real margin math behind it (checked against current Sonnet API pricing, $3/$15 per million tokens — even heavy usage costs low single-digit pounds per subscriber per year), deliberately priced well below the wellness-app category (Headspace/Calm ≈£55-58/yr, Wysa ≈£80/yr) since a meaningful share of Hold's audience is chronically ill people with unstable income, a deliberate choice over raising price toward category norms even though margin would allow it. Two new one-time purchases: an AI credit pack (£2.99, fixed drafts, never expiring, no subscription) as a low-friction entry point before Hold+; a Patterns Report (£2.99, separate IAP product from the credit pack) — Patterns itself remains explicitly not-yet-scoped (what data it draws from, what it shows, whether AI memory data feeds into it), price logged only, no implementation begun. Three fair-access funding mechanisms — "pay more if you can"/supporter tier, gift-a-year, charity partnerships — logged as future-spec, structure captured, in `08-decisions/04-open-questions.md`, alongside BYOK (already tracked there, not duplicated) | Same continuation as today's docked-bar/chip decisions, not a new standalone topic — logging the business side of the same day's work | Active — flags (1) and (2) resolved 2026-08-11 (below): the Founding Member/Standard split and monthly tiers are confirmed removed, not just unmentioned, and the Patterns Report is confirmed as the GP export's replacement, not a coexisting or ambiguous third product. Flag (3) ("No AI, no Patterns" on Free) was not addressed by the 2026-08-11 correction and remains open |
| 2026-08-11 | UI refinement pass on Going Quiet's circle picker and the docked bar, based on on-device screenshots — continuation of yesterday's docked-bar/chip work, not a new topic. `STANDARD_CHIP_DIAMETER` increased again, 64pt/68dp → 72pt/76dp (chosen option, final figure given directly), so a dropdown arrow (" ▼"/" ▲", same technique Manage Circles already used, appended into the measured label) fits on named-Circle chips in both Going Quiet and Manage Circles while still fitting as true circles for common short names — Going Quiet was missing this arrow entirely; a sent-and-collapsed chip keeps its checkmark look instead, no arrow, so the two signals never stack. Simplified the "+ New Circle" flow: tapping "+" now opens the docked bar immediately, focused, no intermediate tap through a separate preview box; the inline suggestion chips and box are gone from Going Quiet's scrollable row entirely. Suggestion chips ("Friends"/"Work"/"Book Club," name-only) moved to their own row directly above the docked bar's input line, visible only while empty — required lifting Circle-creation (`addCircle`) from `GroupPicker` up to the screen, since the docked bar's suggestions (rendered above the keyboard) and `GroupPicker`'s own "Add" button now need to trigger the exact same submission. Dictation mic icon enlarged (20→26). Docked bar's top border thickened and switched from neutral grey to the Strong/primary green token (1px grey → 3px green). Docked bar's fill (and the suggestion chips') changed from a flat surface colour to a computed 60/40 blend of the pale chip green and the app's own border-grey token — `#c8d1c9` in the light-normal palette, `#3a4039` dark-normal, `#d1ccb3` light-quiet, `#413a2a` dark-quiet — deliberately blending two existing tokens rather than inventing a new colour, matching Instagram's own message-composer technique of visually bridging the keyboard and the input field rather than a hard-edged block. Confirm/complete icon on the docked bar: one paper-plane icon everywhere, no exceptions — a context-aware checkmark-for-names variant was proposed mid-thread and explicitly rejected before being built | Continuation of the same day-and-a-half's docked-bar/chip work, logged accordingly rather than as a new standalone topic, per direct instruction | Active — supersedes the 64pt/68dp `STANDARD_CHIP_DIAMETER` figure in `04-ux-content/04-navigation-architecture.md`'s Circle-shape section (now updated with the arrow rule too); supersedes the inline suggestion-chip placement described in `04-ux-content/01-core-journeys.md`'s Text-input-pattern section (now updated); no conflict with the Amend-with-AI section — the checkmark-icon idea was rejected before it was ever written into the book |
| 2026-08-11 | Second, larger same-day refinement pass (14 items) on the same Going Quiet circle picker and docked bar, from further on-device testing — several items explicitly correct the immediately-preceding 2026-08-11 entry above, logged as corrections, not as if that entry never existed. `STANDARD_CHIP_DIAMETER` grown a fourth time, 72pt/76dp → **90pt (iOS) / 95dp (Android)**, final figure given directly; the dropdown arrow is no longer part of this measurement at all, since it's now split into its own independent tap target (below), so the diameter only has to fit the Circle name itself. Named-Circle chips now split into two independent tap targets: the circle (send-selection, unchanged meaning) and a separate small arrow beside it (opens/closes that Circle's card, wired to a new `expandedCircleIds` state in `create/people.tsx` — a sent Circle's card can now be viewed/edited via its arrow without that alone implying a reselect-to-resend, a genuinely new capability, not just a relabelling). Close Circle's distinctive stronger default fill — originally decided 2026-07 ("Circle-pill selection uses a dark green border... with Close Circle given a stronger fill and priority position") and carried into `AdaptiveCircleChip` as the `isPrimary` prop — is **removed**: Close now uses the exact same sage-default/dark-green-sent-fill treatment as every other Circle, with no colour-based distinctiveness at all; a bolder font weight on its label is its only remaining secondary cue, on top of its unchanged fixed-first position as the primary identification mechanism. "Manage your Circles" link removed from Going Quiet entirely (confirmed via grep it was the link's only render site app-wide — nothing else depended on it). Fixed a real gap behind item 7's stated concern: `period.audienceCircles` was only ever written once, at the very first Send — a Circle added to the selection afterward, with no further Send that session, would never be synced into it, meaning Reconnect's "add permanently?" prompt would never fire for that person and they'd silently fall out of tracking; a new `syncAudience()` in `holdHistoryService.ts` now re-syncs the full current selection at Going Quiet's "Done" step, independent of whether another Send happens first. The old "+"-flow Add/Cancel buttons are gone — the docked bar's own paper-plane commits (`onDone`), and a new generic `onDismiss` (wired to `Keyboard`'s `keyboardDidHide` event, defaulting to `onDone` when a screen doesn't need different behaviour) closes the bar without committing on any tap-outside, for every field on every screen this bar serves, not just Circle-naming. "+" itself now renders as just the glyph (no baked-in text, matching the same fit reasoning as named circles), noticeably larger/bolder than the small arrow glyph beside named chips, with a separate "New Circle" caption beneath it while active. `AdaptiveCircleChip` had a real bug fixed as part of this: its selected-ring style was gated on `!outline`, so an outline chip ("+") could never show a ring at all regardless of `isSelected` — confirmed by direct code read, not assumed — replaced with a dedicated `chipSelectedOutline` treatment (filled tint + 3px border) driving "+"'s active/open state; tapping "+" again while active, or tapping outside, closes it via the same generic dismiss path above. "+" and "All" confirmed to never use the dark-green sent-fill (neither has a sendable target of that kind); "All"'s existing selection ring is left as-is, already at standard weight. Docked bar redesigned Instagram-style: the mic and paper-plane icons now sit inside the text field's own rounded pill as one continuous shape, not three separate row siblings; suggestion chips ("Friends"/"Work"/"Book Club") changed from circle-styled Pressables to a plain text list separated by thin vertical divider lines, at 16pt (matching the input's own font size, within the documented 15-17pt Compact-text range) — correcting yesterday's circle-styled suggestion chips, which read as competing with the actual Circle chips they exist to help create. Two-tier colour spec: inside the pill, kept yesterday's existing chip-green blend, but with the theme's own text colour (not a fixed white) — computed WCAG contrast against the theme's defined hex values: light-normal (`#c8d1c9` fill + `#242825` text) ≈ 9.55:1, dark-normal (`#3a4039` fill + `#edefe8` text) ≈ 9.19:1, both well past AA's 4.5:1; outside the pill, the fill now blends mostly toward an approximated system-keyboard grey (`#D1D3D6` light / `#2C2C2E` dark — reasoned approximations, not sampled from a real device) rather than the app's own border/background tokens, correcting yesterday's blend, which read only toward the app's own palette. Yesterday's 3px green top border is removed entirely — it read as a jarring, heavy line on-device — replaced with rounded top corners (24pt, `theme.radius.lg`, a reasoned choice, not a measured one) instead of any border. Root-caused and fixed the docked bar's still-open "not flush to the keyboard" gap structurally, in `Screen.tsx`: the bar's `KeyboardStickyView` was nested inside a bottom-inset `SafeAreaView`, so its natural (keyboard-closed) resting position sat above the true screen bottom by the home-indicator inset; `KeyboardStickyView` translates its child by exactly the keyboard's own pixel height, which doesn't compensate for that inset, leaving a gap equal to it once the keyboard was open. `dockedInput` now renders as a sibling outside that inset-padded SafeAreaView, and `DockedInputBar`'s own inner `SafeAreaView` dropped its redundant "bottom" edge (the keyboard is always what's directly beneath it whenever it's visible) — reasoned from the library's own transform implementation, not verified against a live device measurement. Ran the required name-fit audit at the new 90pt/95dp diameter (`CIRCLE_FIT_PADDING` = 10pt each side, so 70pt/75pt available): at default (14pt semibold) size, estimated text widths (~0.58×fontSize per character, SF Pro Semibold approximation, not device-rendered) put Close, Friends, Work, All, and typical short user-created names (e.g. "Family," "Mum") comfortably inside the circle threshold on both platforms; "Book Club" (9 characters incl. space) sits right at the boundary — pill on iOS, borderline circle on Android by this estimate, a genuine close call flagged rather than asserted; longer user-created names (e.g. "College Friends") clearly exceed it and render as pills. At the largest Dynamic Type accessibility size (~43pt effective, ~3.1× base), essentially every name — including "Close" and "All" — becomes a pill; this is the intended circle-to-pill degradation working as designed, not a bug, and no name is shrunk or truncated at any size, since chips use `numberOfLines={1}` with an unconstrained pill width and a fixed chip height taller than the largest accessibility line height — confirmed structurally, not by rendering the actual text. None of these text-width or contrast figures were measured on a live device; all are calculated from the theme's own defined values and reasoned approximations, flagged as such throughout, consistent with this session's repeated lack of simulator/pixel-measurement access | Several items directly correct choices made in the entry immediately above, after seeing them on-device, per direct instruction — logged as corrections to preserve why the earlier entry existed, not to erase it. The Close-Circle-colour reversal corrects a 2026-07 decision, not a same-day one, flagged separately for that reason | Active — supersedes the 72pt/76dp `STANDARD_CHIP_DIAMETER` and appended-arrow technique in `04-ux-content/04-navigation-architecture.md`'s Circle-shape section (now updated); supersedes the 2026-07 "Close Circle given a stronger fill" line in this log (line 23) — Close's priority position stands, its colour distinctiveness does not; supersedes the immediately-preceding 2026-08-11 entry's top-border and app-palette-only blend description for the docked bar, and its circle-styled suggestion chips, all now updated in `04-ux-content/01-core-journeys.md`'s Text-input-pattern section |
| 2026-08-11 | Pricing correction, per direct instruction, resolving three items flagged open in this log and in `07-business/02-pricing-principles.md`: (1) **Founding Member/Standard tier split removed entirely**, including both monthly cadences (£1.99 and £2.99) — supersedes the 2026-07 "Revise pricing to a Founding Member launch offer" row above (line 24) and the 2026-07 "Reaffirmed £29.99/year standard Hold+ pricing" row (line 77). One Hold+ subscription price now: **£17.99/year**. (2) **GP/clinician PDF export and Patterns Report merged into one product** — supersedes the 2026-07 export-split row (line 76) and the 2026-07-29 "corrected the £3.99... to its own standalone section" row (line 116), and resolves the "Flagged, not resolved" Patterns Report ambiguity in the 2026-08-11 pricing-decision row above (line 166, flag 2): one paid, one-time purchase, **Patterns Report, £2.99**, replacing both the £3.99 GP/clinician export and the previously-unreconciled £2.99 Patterns Report price. Reframed as not medical-only — useful for a GP/clinician appointment if needed, but pitched as a general summary of someone's own patterns, not a document defined by that one use case. (3) **New £4.99/3-month billing cadence added**, alongside (not replacing) the £17.99/year price — a genuine second, lower-commitment option, no trial, no auto-conversion. **Confirmed unaffected by this correction:** free-tier basic Patterns stays free, richer Patterns stays a Hold+ subscription benefit (per `03-product/04-patterns.md`); the £2.99 AI credit pack is a separate product, untouched; "no free trial on any tier" (`02-pricing-principles.md`, "Ethical access options") already covered the new tier structure without needing a change, and is now confirmed explicitly against it | Direct instruction to resolve all three items the previous status check confirmed were still open, rather than leaving them as standing flags indefinitely | Active — supersedes the Founding Member/Standard table and both monthly cadences in `07-business/02-pricing-principles.md` (row 24 above); supersedes the £3.99 GP/clinician PDF export naming and price in `07-business/02-pricing-principles.md`, `07-business/06-business-strategy.md`, and `03-product/04-patterns.md` (rows 76 and 116 above); resolves flags (1) and (2) of the 2026-08-11 pricing-decision row above (line 166) — flag (3) of that row ("No AI, no Patterns" on Free) is untouched by this correction and remains open; also corrected stale "Founding Member" references in `03-product/05-roadmap.md`, `04-ux-content/04-navigation-architecture.md`, and `08-decisions/04-open-questions.md` |
| 2026-08-11 | Third same-day pass on the Going Quiet circle picker and docked bar (four items, corrections from further on-device testing of the entry directly above), plus a separate bug report on Going Quiet's completion area, investigated and fixed the same turn. Dropdown arrow moved from beside the circle to inside it, right-aligned and overlapping the chip's own right edge, since floating outside the circle read as ambiguous about which one it belonged to — still an independent tap target, now with a small scrim badge behind the glyph for visual separation from the label underneath it. Docked-bar pill/outer colours corrected a second time — the first correction's blend still read as uniformly too dark on-device, particularly in dark mode, where both source tokens were themselves dark, leaving no lightness contrast between the pill and its surround; asked to sample Instagram's actual composer colours directly, but this session has no way to open Instagram or read real device pixels, and a web search surfaced only unverified fan-made clone projects, so that was flagged rather than treated as a real sample — instead made a reasoned, principled adjustment (light mode leans the pill toward white, dark mode leans it toward the app's own vivid accent green rather than its dark tokens, so the pill reads as a lighter accent against a darker surround, the relationship the previous value had inverted). Recomputed contrast: light-normal ≈12.8:1 (up), dark-normal ≈6.0:1 (down from ≈9.2:1 but still past AA's 4.5:1) — a real, reported trade-off of the fix, not silently absorbed. AI-amend's sparkle icon moved inside the pill alongside the mic and send icons (icon choice itself unchanged — confirmed as the established cross-industry "AI action" convention); caught in passing that the send button's tap target had been shrunk below the documented 44pt accessibility floor in the previous pass without being flagged, fixed with `hitSlop`. Docked-bar input text confirmed at 16pt, matching the documented 15-17pt Compact-text range, not a smaller size specific to that component. Separately: Going Quiet's page-level Send button was reported as greyed out, suspected to be unwired leftover architecture now that "sending happens per-circle via the docked bar." Investigated and corrected that premise — the docked bar's own paper-plane has never sent anything; it only closes a text field already autosaving locally. The actual bug was different but real: the single page-level Send button required *every* visible Circle's message to be non-empty before it would enable at all, so one Circle still mid-intent-selection made Send untappable even for a different, fully-written Circle beside it. Fixed by giving each Circle's card its own independent Send button (matching the pattern Reconnect/Taking Time's update/Library's Quick message and this same screen's own personal notes already use), removing the shared page-level button entirely. Checking whether this left a way to exit Going Quiet without sending anything surfaced a second, genuinely pre-existing gap, not a leftover from today's change: "Done" had only ever been reachable once at least one Circle had been sent — there was no way to leave Going Quiet at all without sending something first. Fixed with an unconditional "Done" shown in Send's old position before anything's been sent. `04-ux-content/01-core-journeys.md` points 4 and 7 updated to match, both marked explicitly as corrections to their own same-day-earlier phrasing rather than rewritten as if the earlier description had always said this | Corrections from on-device testing, logged as continuations/corrections of the entries above, not new topics; the Send-button item started as a bug report with an incorrect premise about existing behaviour, corrected rather than built on top of, and surfaced a second real gap (Done's reachability) beyond the one originally reported | Active — supersedes this same day's earlier docked-bar colour values and AI-amend placement, both now updated in `04-ux-content/01-core-journeys.md`'s Text-input-pattern section with the corrected figures; supersedes point 4's "Send fires for whichever cards are currently visible" (single shared trigger) and point 7's "reachable as soon as at least one Circle has been sent" (Done's minimum reachability) in `04-ux-content/01-core-journeys.md`, both now updated in place with the correction visible, not silently rewritten. The Instagram colour-matching instruction itself remains genuinely unresolved — flagged, not silently approximated as if it were satisfied |
| 2026-08-11 | Content-only pass across `02-research/` and `04-ux-content/`, three of four requested items completed, per direct instruction: (1) **Elevated shame alongside guilt** in `02-research/06-guilt-spiral-and-supportive-language.md` — added a new "Shame and guilt are not the same thing" section establishing the distinction (shame: negative evaluation of the self as a whole, motivates withdrawal; guilt: negative evaluation of a specific behaviour, can motivate repair) and a new voice rule explicitly flagging identity-level language ("you've been neglecting people") as a distinct, at-least-equally-serious failure mode from behaviour-focused guilt language, with worked examples. (2) **Added content-depth-by-Circle-type guidance** to `04-ux-content/02-voice-and-language.md` — Close/Core starter content can be fuller/more personal by default, Friends lighter, Work most minimal, per disclosure-decision-making research; explicitly scoped as a default-suggestion behaviour for template starters and onboarding copy only, never an enforced restriction on what a user can actually write. (3) **Logged the adult-only scope boundary** in `08-decisions/04-open-questions.md` — a youth/family variant is out of scope by deliberate design decision, not an unnoticed gap, since pediatric chronic-illness self-management research indicates it would need a fundamentally different multi-person/family-inclusive design rather than a scaled-down single-user model. **Not completed: item (4), the full research batch (~30 sources spanning social connection, loneliness intervention, habit formation, disclosure, shame/RSD, self-compassion and more) for `02-research/`.** The instruction pointed to citation/DOI detail in "the session write-up document," which does not exist anywhere in this repository or the working scratchpad — searched and confirmed absent. Citations and DOIs were not fabricated from memory for a research document; this item is blocked pending the user supplying that source document or the citation list directly | Items (1)-(3) were self-contained content edits using research framing already supplied directly in the instruction (Tangney et al. for shame/guilt; Joachim & Acorn, Chaudoir & Fisher for disclosure) and didn't depend on the missing document; item (4) explicitly requested "exact citations as compiled" from a named source that couldn't be located, and inventing academic citations/DOIs for a document meant to carry real research weight would be a worse outcome than asking | Active — items (1)-(3) complete; item (4) blocked, not silently dropped, pending the source document |
| 2026-08-11 | Completed the previously-blocked item (4): added the full ~30-source research batch to a new `02-research/07-extended-evidence-base.md`, organised by theme (social connection/mortality, weak ties, reach-out underestimation/liking gap/compliance underestimation, habit formation, loneliness interventions, plan-making/Zeigarnik/implementation intentions, perfectionism/procrastination in chronic illness, active vs. passive social media use, time perception in chronic pain, self-perceived burden, friendship navigation, network bridging/narrowing, reciprocity, disclosure decision-making, AI-writing authenticity, Dunbar's layered relationships, patient activation, shame vs. guilt, the shame spiral, ADHD/RSD, denial/self-esteem in chronic illness, colour-mood research, Shame Resilience Theory, self-compassion), sourced from the actual session write-up document once supplied, not from memory. Every finding tagged Causal, Correlational, Descriptive/qualitative, Theoretical, or Meta-analysis (mixed designs), per direct instruction not to present observational associations as causal claims. Confirmed the two previously-circulating unverified stats ("60% avoid guilt spiral," "80% love hearing from someone") do not appear anywhere in the real batch and restated, again, that they must not be used anywhere. Cross-referenced the new file from `02-research/01-research-map.md` (new "Extended evidence base" pointer section), from `02-research/06-guilt-spiral-and-supportive-language.md` (updated the shame/guilt attribution placeholder added in the entry directly above, now pointing at the real source instead of promising a future addition), and from `04-ux-content/02-voice-and-language.md` (added the full Joachim & Acorn / Chaudoir & Fisher citations to the content-depth-tiering section logged in the same entry). **Three entries flagged as needing a citation check before the Research page treats them as fully sourced, per direct instruction — not fully cited even in the original source document, not just in this write-up:** Sirois (2014, procrastination/guilt in chronic illness — journal/volume/DOI missing), Hibbard et al. (patient activation framework — no citation detail at all), and Neff (Self-Compassion Break — full citation missing, plus an unspecified health-professions-specific study referenced but never detailed in the original compilation) | Direct instruction, source document supplied this turn after being confirmed absent from the repo last turn; citations transcribed as given rather than embellished, with correlational/causal tagging added as an explicit, separately-requested judgement call on top of the source material, not part of the original compilation | Superseded 2026-08-11 (below) — all three citations completed via direct web search; two carry confidence caveats, see the entry below |
| 2026-08-11 | **Going Quiet redesigned from batch multi-select/per-Circle-cards to sequential, group-based sending, with combination-keyed saved templates — a full architecture change, done after an explicit reconciliation pass against current state, per direct instruction.** Reconciliation found real conflicts rather than building fresh on top of them: the per-Circle cards (with last turn's own independent per-Circle Send buttons) were confirmed as drift against "exactly one text-entry surface," confirmed for full removal; the request's "Send/Edit/Personalise + Conversations-dropdown pattern already built for Reconnect" didn't literally exist in `reconnect.tsx` (which only ever did a plain nav-away hand-off) — the real rich mechanic lived in Library's `PersonaliseAccordion`, extracted into its own file plus a new `usePersonaliseCompletion` hook so Going Quiet's and Reconnect's completion steps could both reach it in place, confirmed as the intended answer rather than picked silently; `app/create/review.tsx`, named in the request, didn't exist anywhere and was confirmed to fold inline into `people.tsx` instead, matching how the screen already worked; the "bundle removed people into a new Circle" mechanism named as a reuse didn't exist either — built new, on the existing pending-Circle/`toggleGroup` conventions specifically so the already-fixed "Reconnect prompt fires regardless of send" behaviour (`syncAudience()`, fixed two same-day passes earlier) covers it automatically. **Generalised "All":** any selected subset of Circles is one group sharing one message and one Send, matching the flat-selection + shared-message shape `app/return/update.tsx`'s "Send an update" already used — not a hypothetical pattern, a proven existing one. Send clears the selection back to empty afterward so the next group can be picked; repeat until Done or every Circle is covered. **Combination-keyed templates:** `templateService.ts` extended with a second, parallel key scheme (`combinationKey` — sorted, joined Circle ids) alongside the untouched single-Circle one; a previously-sent-to combination auto-loads its saved default, a new one offers starting points drawn from its member Circles' own single-Circle defaults instead of auto-loading anything, and silently saves its own default the moment a message is actually sent for it (no manual "Save" control offered for combinations — that stays strictly single-Circle, unchanged). **"Done" gating** (reaffirmed, already implemented in an earlier same-day pass): unreachable until at least one message has been sent. **Provisional Circle-split:** one or more already-removed people, spanning more than one Circle's list, can be bundled into a new provisional Circle via a checkbox + "+ New circle from N selected" — auto-generated placeholder name, no naming prompt mid-flow; a new `recipientCircleOverrides` map in `HoldFlowContext.tsx` (and a corresponding fix to `mergeGoingQuietRecipients`, which would otherwise keep silently reattaching a split person to their original Circle every time it recomputes, since their real Circle membership is never touched) makes the reassignment actually stick for the session. **Reconnect's own completion step upgraded in the same pass**, not left inconsistent: its plain "Personalise" nav-to-Library button now opens the same in-place accordion — confirmed as the real gap, not a pattern worth preserving, since Reconnect's own `send()` already seeds its whole audience into Conversations regardless of this choice, so there's nothing to seed here, only to read back. `GroupPicker.tsx` simplified in the process — the reselect/expand-arrow machinery from two same-day passes earlier is now genuinely obsolete (it existed to solve problems — an independently-persisted draft per Circle, viewing a sent Circle's card without reselecting — that don't exist once every Circle-combination shares one message) and was removed, not just left unused. `tsc --noEmit` and `vitest run` (27/27) both pass throughout | Direct instruction, with an explicit mandatory reconciliation-first step given the scale of the change and how recently the per-Circle-card architecture had been built (three same-day passes) — the reconciliation surfaced real conflicts and factual corrections to the request itself (Reconnect's actual current behaviour, `review.tsx`'s non-existence, the bundle mechanism's non-existence) rather than silently building on false premises | Active — supersedes the per-Circle-card/independent-Send-button architecture described in the three preceding same-day entries above, and the "Send fires for whichever cards are currently visible" / per-Circle draft framing in `04-ux-content/01-core-journeys.md`'s Going Quiet section (now fully rewritten); supersedes Reconnect's own "Personalise leads into Conversations, unchanged" framing in the same file; `03-mvp-requirements.md`'s Going Quiet and Reconnect P0 lists, and `04-ux-content/04-navigation-architecture.md` (new "Personalise pattern" section), both updated to match |
| 2026-08-11 | Completed the three citations flagged in the entry above (Sirois, Hibbard et al., Neff) in `02-research/07-extended-evidence-base.md`, found via direct web search against real academic sources, not reconstructed from memory. **Sirois:** both Sirois (2014, *Self and Identity* 13(2), 128–145, procrastination/self-compassion) and Sirois, Molnar & Hirsch (2015, *Self and Identity* 14, 334–347, chronic-illness-specific) now cited, since the section's own framing blends both a general-procrastination and a chronic-illness angle — a judgement call about which paper(s) match the finding as used, not a single unambiguous match, flagged as such rather than picking one silently. **Hibbard et al.:** Hibbard, Stockard, Mahoney & Tusler (2004), *Health Services Research* 39(4 Pt 1), 1005–1026, DOI 10.1111/j.1475-6773.2004.00269.x — the field's foundational, most-cited Patient Activation Measure paper, high confidence. **Neff:** Neff & Germer (2013), *Journal of Clinical Psychology* 69(1), 28–44 (the Self-Compassion Break RCT, high confidence) plus Neff, Knox, Long & Gregory (2020), *Journal of Clinical Psychology* 76(9), 1543–1562, for the previously-undetailed health-professions-specific study — this second citation is a good-faith subject-relevance match, not a certain one, since the original reference that prompted it was too vague to confirm with full confidence; worth a final check before anything published-facing (e.g. the in-app Research page) treats it as settled. Also updated `02-research/01-research-map.md`'s pointer to reflect completion | Direct instruction, citations supplied this turn from real web search results rather than reconstructed from memory; both confidence caveats (the Sirois two-paper judgement call, and the Neff health-professions match) preserved explicitly per instruction rather than smoothed over now that the "flagged" status is otherwise resolved | Active — supersedes the citation-check flag on these three entries in the row above; the two confidence caveats above are not fully resolved and should be re-checked before the in-app Research page treats them as settled |
| 2026-08-11 | Two corrections to the entry immediately above, per direct follow-up instruction, logged as corrections rather than a silent rewrite. **Sirois:** the two-paper citation (2014 procrastination-focused, 2015 chronic-illness-focused) is **not** a hedge or an ambiguous judgement call as the prior entry framed it — both apply deliberately, since Hold's content draws on both the procrastination-avoidance mechanism and the chronic-illness-specific framing; the "judgement call, not a single unambiguous match" language is retracted. **Neff:** the prior entry's single Self-Compassion Break citation (Neff & Germer 2013 alone) was incomplete — expanded to the exercise's full lineage, each citation scoped to its own purpose: foundational theory (Neff 2003a, *Self and Identity* 2, 223–250, the self-compassion scale; Neff 2003b, *Self and Identity* 2, 85–102, the conceptual paper) for explaining the underlying concept; the practical evolution into the named exercise (Neff & Germer, *The Mindful Self-Compassion Workbook*, Guilford Press, also documented at self-compassion.org — practitioner/teaching material, not a peer-reviewed study, cited as the exercise's origin, not as evidence it works) for describing what the Self-Compassion Break is; and the 2013 RCT (unchanged from the entry above) for any claim that practicing it is effective. The separate 2020 health-professions citation and its confidence caveat are unchanged by this correction. `02-research/07-extended-evidence-base.md` and `02-research/01-research-map.md` both updated to match | Direct follow-up instruction arrived before this turn's response was sent; corrected in place in the research file itself (per how the rest of this log treats same-session corrections — a new row, not a silent edit to the entry above) rather than treated as a private draft revision | Active — supersedes the Sirois "judgement call" framing and the single-citation Neff treatment in the row immediately above; the Neff 2020 health-professions confidence caveat still stands, untouched by this correction |
| 2026-08-11 | **Going Quiet moved from generalised-All to a session-scoped queue; Personalise removed from Going Quiet entirely, replaced by an ad-hoc "spin removed people into a new Circle" mechanic; two real "All"-selection bugs found and fixed; Reconnect's own sent-state chips and completion routing corrected.** Investigated first, per direct instruction, before changing anything: the dropdown arrow's disappearance was a genuine regression (a prior pass removed it along with reselect machinery that was actually obsolete, but the arrow's own purpose — viewing a Circle's members on demand — wasn't); "All" corrupting selection had two distinct causes, an empty-Circle filter silently dropped in a prior rewrite, and a genuine race condition in a bulk-toggle implementation built from N sequential individual toggles reading potentially-stale selection state (fixed with one atomic selection-replace, not a loop); a reported "Transition screen appears before Personalise" bug turned out not to exist — `return/transition.tsx` only ever fires at the very start of Reconnect, before Personalise/OOO are even reached — but the investigation surfaced a real, related gap instead: Reconnect had no unconditional completion/rest screen of its own, unlike Going Quiet's `create/done.tsx`, going straight home instead. **Queue model:** selecting a Circle now joins both the current message group and a session-long queue that only grows; Circles active for the message being typed float to the front of the row (deliberate, confirmed); once every queued Circle has been sent to, the flow completes automatically, with "Done" becoming specifically the early-exit action rather than the normal way to finish. **Personalise removed from Going Quiet** (kept, unchanged, in Library and Reconnect) — replaced with a screen-level roster of everyone removed from any Circle's dropdown this session, bundleable via "+" into a new provisional Circle, with people already claimed by an earlier bundle shown in a distinct muted state and re-addable to a further bundle without cancelling their existing message (flagged: the app's existing phone-number-dedupe send architecture can't currently guarantee literal delivery to *both* circles for someone re-added this way — a real limitation of this pass, not silently resolved). Circle member lists are now hidden by default, shown only via a restored (now single-expand-only) dropdown arrow, which also gained a new "+ Add person" entry point — the first such entry point anywhere in Going Quiet itself, reusing Settings' Manage Circles' own underlying contact-picker/storage calls. Docked-bar send now dispatches immediately for Going Quiet's message field specifically (a deliberate, explicit exception to the app-wide "paper-plane only ever means done editing" rule), and its icons moved from the bottom of the pill (where they clipped the rounded corner) to the top. Reconnect: sent-state chips lost their checkmark (confirmed as the actual cause of a reported pill-inflation bug — the checkmark was baked into the measured label text, tipping the circle-fit check over for long enough names) in favour of colour-only fill, matching the standard elsewhere; completion now always ends at `return/done.tsx`, correcting the asymmetry found during investigation | Every fix and mechanic here traces to either a specific on-device bug report investigated to a real root cause, or a section of a long, explicit instruction that itself required a reconciliation pass against current code before building, per direct instruction not to build silently on an assumption | Active — supersedes the per-Circle-card-era arrow/reselect description in `04-ux-content/04-navigation-architecture.md`'s Circle-shape section (now corrected in place, not rewritten as if it never existed) and the immediately-preceding "Personalise pattern — shared across Library, Going Quiet, and Reconnect" section heading (Going Quiet's own inclusion retracted, corrected same-day); supersedes Going Quiet's numbered list in `04-ux-content/01-core-journeys.md` (fully rewritten for the queue/roster model); full technical detail (exact file/function names, the specific data-model conflict around re-adding a claimed person to a second bundle) lives in `hold-app`'s own `docs/09-decision-log.md`, not duplicated here |
| 2026-08-11 | Logged two Wider World items to `08-decisions/04-open-questions.md`, per direct instruction, alongside this pass's built Email and Status channels. **Future spec, not built (three items):** calendar auto-blocking during Taking Time (needs real calendar API/OAuth integration, meaningfully bigger than a text field); SMS auto-reply as a ready-to-paste manual fallback only, since genuine native auto-reply isn't available to third-party apps on either platform without disproportionate scope; text-to-speech voicemail greeting generation, blocked by the lack of any public carrier-voicemail-setting API on either platform, so would require manual application by the person or a much larger third-party voicemail-service integration. **Closed, not open (one item):** Hold becoming Android's default SMS handler to enable native auto-reply was investigated and rejected — Google Play policy requires full default-SMS-handler status (every text sent/received/read on the device) with no scoped-down permission tier limited to auto-reply alone, which would be a disproportionate, permanent expansion of Hold's data access directly contradicting its privacy-by-design principle; not possible on iOS at any scope, since Apple has no third-party default SMS handler concept at all and Driving Focus (the only native auto-reply mechanism) is OS-exclusive. Marked explicitly closed, not deferred, so it isn't quietly revisited without this context | Direct instruction to record both the future-spec list and the closed decision together, keeping the closed item legibly distinct from every other (genuinely open) entry in that file | Active — the three future-spec items remain open, not committed; the SMS-handler item is closed and should not be revisited absent a materially different platform policy landscape |
| 2026-08-11 | Logged five planned-but-not-built specs across the book, per direct instruction, each marked explicitly as planned/not implemented in hold-app. (1) **Draft retention architecture** — `06-privacy-security/04-content-retention.md`: 30-day retention for Going Quiet/Reconnect drafts, 7-day for Conversations reply drafts (paired friendMessage on the same lifecycle), a quiet pre-clear heads-up, and a hard "Save to Library never saves the friend's message" rule. **Flagged, not silently reconciled:** this directly conflicts with the same file's existing built model (48-hour draft backstop, 4-hour pasted-message window) — both are documented, neither has been chosen. (2) **AI drafting quality bar** — `06-privacy-security/02-ai-boundaries.md`: a concrete 2–3-template same-Circle-first style-reference rule (sharpening the existing general personalisation line), plus a new explicit anti-AI-phrasing instruction requirement not currently in any built system prompt. (3) **Accessibility audit scope** — `02-research/04-accessibility.md`: planned Dynamic Type, touch-target, and accessibility-label audits, the last being the intended lever for OS-level Voice Control/Voice Access rather than a custom in-app voice system; explicitly excludes Dark Mode/Reduce Motion, already specified in `docs/08-product-bible.md` (in `hold-app`). (4) **Transition screen copy + citation marker mechanism** — `04-ux-content/04-navigation-architecture.md`: resolved that section's stale "not yet decided whether this becomes a real screen" framing (it's built, per `04-ux-content/01-core-journeys.md`), then added drafted replacement copy for both the Going Quiet and Reconnect transition moments plus a Research-page citation-marker mechanism (anchored sections, "Why this is true" tap targets on the "common humanity" line only) — flagged as a proposal against currently-shipped copy, not an approved replacement. (5) **Wider World — Email and Status** — `04-ux-content/01-core-journeys.md`: full spec for the mocked-only OOO/status feature referenced elsewhere in that file — provider-API email toggling with a manual-paste fallback, and a status-platform checklist that resurfaces at Reconnect for take-down tracking | Direct instruction to log all five as documented plans, explicitly distinct from completed work, so none is later mistaken for something already built | Partially superseded 2026-08-11 (below) — item (1)'s conflict is resolved in the row below; items (2)-(5) unaffected and still stand as written; none of the five items are implemented in hold-app |
| 2026-08-11 | Resolved item (1) of the entry above, per direct confirmation: the day-based retention windows (30-day Going Quiet/Reconnect, 7-day Conversations reply drafts with paired `friendMessage`) are the current, intended policy — the existing 48-hour/4-hour model in `06-privacy-security/04-content-retention.md` was an earlier draft, now superseded, not a separate later decision that could coexist with it. Struck through the superseded hour-based tiers in place (kept for the record, not deleted, matching how other superseded material is handled elsewhere in this book) rather than leaving both documented as if still open. Three downstream sections in the same file also referenced the now-superseded numbers and needed updating for internal consistency, not just the tiers list itself: "Graceful behaviour, not an error state" (its whole premise — the paste box clearing before the reply — can no longer happen once both share one 7-day lifecycle, struck through and marked superseded); "Disclosure, not pressure"'s example reassurance copy ("clears sooner") no longer describes the resolved policy, struck through with replacement copy flagged as not yet drafted; "Why 'fully closing' rather than a pure timer" is no longer applicable at all, since the resolved policy has no separate app-close trigger for the friend's message specifically — struck through and marked superseded rather than left as if still active reasoning | Direct instruction confirming which model is correct, plus a consistency pass this required — leaving the downstream sections un-updated would have recreated the exact "both documented as if still open" problem the instruction was explicitly trying to fix, just one level deeper in the same file | Active — `06-privacy-security/04-content-retention.md` now has one resolved retention policy, not two; still not implemented in hold-app. **Reassurance copy gap closed the same day, per direct instruction:** the old "clears sooner" example was fully rewritten, not just trimmed, to "Your reply and their message both stay on your device for about a week in case you need to step away, then clear together automatically." — accurately reflecting the paired 7-day lifecycle rather than leaving a partially-edited, still-inaccurate sentence in place |
| 2026-08-11 | Finalised the "Disclosure, not pressure" reassurance copy in `06-privacy-security/04-content-retention.md`, per direct instruction — this is the settled copy, not a further draft. Supersedes the same-day interim rewrite ("Your reply and their message both stay on your device for about a week in case you need to step away, then clear together automatically."), which is now struck through alongside the original superseded "clears sooner" version rather than replaced in place, so both prior attempts stay visible in the file's history. **Final copy:** *"This and their message stay private to your device for about a week, then clear together. Nothing to manage."* | Direct instruction marking this as final, not another iteration — logged as such rather than with the "flagged, not yet settled" language the interim version carried | Active — this is the shipping copy for `06-privacy-security/04-content-retention.md`'s Personalise-save reassurance mention; still not implemented in hold-app, same as the rest of the 30-day/7-day retention policy it belongs to |
| 2026-08-11 | Consolidated 17-section pass (numbered 0-16), worked in stated priority order, section 0 first since it corrected a wrong premise a previous instruction had built on. **Delivery model corrected: individual/BCC-style by default, not one shared group message** — the earlier instruction to send combinations as one shared thread was wrong and is superseded, not built on further; each recipient now gets their own separate message by default (no one in a Circle knows who else received it), with a per-Circle "send as group" toggle, default off, available at Circle creation and in that Circle's own Manage Circles settings; a combination with mixed per-Circle settings still sends as one action, each Circle following its own setting within it. Confirmed technical consequence: `expo-sms` has no silent-send capability, so an individual-delivery Circle of N people means N separate native compose-sheet taps per Send, not one. Reconnect brought to parity with Going Quiet across the rest of the list: immediate send from the docked bar (was only closing the field, not sending — the same bug Going Quiet had already fixed), reorder-and-grey-out tied to the one moment of entering the message box rather than every selection tap, a manual "Done" early-exit button, and removal of an "X of Y reached" header line that violated the app's own no-badges-no-counts rule. **A real bug found and fixed, not just a symptom:** Going Quiet's completion step was re-syncing the Hold period's audience from the current on-screen selection, which by that point had already been cleared back to empty by the preceding Send — silently truncating every previously-sent Circle from the record Reconnect's own coverage check depends on entirely; fixed to read from the full session queue instead. Two items investigated and confirmed as **not** bugs against current code (Reconnect never had a "+"/"New Circle" affordance for "All" to wrongly include). Two items investigated and left genuinely unresolved, flagged rather than guessed at: Your Circles' row spacing (content-driven, not a spacing bug) and a reported scroll failure on three settings screens (no code-level cause found despite three independent checks). One item resolved to "already correct, no change needed": OOO/status was already addressable on the same screen as Done/finish before completion could be reached, on both flows, collapsed by default — matching the recommendation given, called out for confirmation rather than assumed settled. Two new open questions logged, not decided silently: whether Reconnect needs its own save/template mechanism (it has no per-Circle template concept to build one against), and a related, previously-unreported instance of the same no-counts violation still live on Home's own "Continue reconnecting" widget. Full technical detail (exact file/function names, the `queuedGroups` fix, the `sendToCircles` fan-out implementation) lives in `hold-app`'s own `docs/09-decision-log.md`, not duplicated here | Every item traces to either a specific section of a long, explicit instruction that named its own priority order, or a bug investigated to a real root cause rather than patched at the symptom — per the same "investigate and report before building" discipline established in the entries above | Active — supersedes the shared-group-message delivery model implied by whichever earlier instruction introduced combination sends (not itself a row in this log); corrects `04-ux-content/01-core-journeys.md`'s Going Quiet points 1, 5, 6, 7 and Reconnect points 1, 3, 4, and `04-ux-content/04-navigation-architecture.md`'s dropdown-arrow and docked-bar-corner-radius sections, all updated in place |
| 2026-08-12 | Resolved the `expo-sms` no-silent-send flag raised alongside the individual/BCC delivery-model correction (2026-08-11, row above): **N pre-filled compose-sheet confirms per Send, one per recipient, is accepted as a settled trade-off, not a bug to fix further.** Two alternatives were investigated and rejected: (1) Android-only silent send via `SEND_SMS`/default-SMS-handler status — requires the same full default-SMS-handler permission already investigated and rejected elsewhere in this project on privacy-scope grounds (permanent visibility into every text on the device), and is iOS-incompatible at any scope, which would create inconsistent cross-platform behaviour even if pursued; (2) a server-side SMS gateway (Twilio-style) — the only genuinely silent, one-tap batch-send option on both platforms, rejected for real ongoing per-message cost and, more importantly, because messages would arrive from an unfamiliar business number rather than the person's own phone number/contact identity, working against Hold's entire premise of a message recognisably from someone the recipient knows. **Why the current approach is acceptable, not just tolerated:** each compose sheet arrives pre-filled with recipient and message already populated, so this is N single confirming taps, not N full compositions, and doubles as a genuine final check before each message actually sends. The already-built per-Circle "send as group" toggle is the accepted mitigation for Circles where N taps becomes burdensome — individual/BCC stays the default for small, close Circles where the extra care is worth it, group delivery is an explicit opt-in for larger ones. Added to `04-ux-content/01-core-journeys.md` alongside the delivery-model decision itself (Going Quiet's Delivery point and Reconnect's Send point), not left as a standalone technical note disconnected from the product spec it explains | Direct instruction to close out a flag raised in the prior pass's investigation, with the alternatives and their rejection reasons preserved so this isn't silently revisited without the context for why it was rejected | Active — this closes the `expo-sms` flag from the 2026-08-11 delivery-model row above; no further investigation planned absent a materially different technical option becoming available |
| 2026-08-12 | **Reconnect's post-coverage-complete state folded into one continuous screen; a real sent-fill regression fixed app-wide; Home's own remaining count removed (not just logged this time); Conversations rebuilt around a shared per-person reply-pill row.** Reconnect used to swap its entire `<Screen>` tree once every Circle/contact was reached — no route change, but different enough (new header, circle row gone, different actions) to read on-device as a separate page. Merged into one screen: the circle row now stays visible throughout; the docked text box's vacated space becomes an inline "Personalise / Not now" choice once nothing's left to compose, with "Not now" collapsing that choice (reopenable, dropdown-arrow style) rather than finishing Reconnect the way it used to — "Done" (already built as the early-exit action) took over as the one way to actually leave, now shown whenever anything's been sent, not just pre-completion. OOO/status's default expand state flipped to open specifically at this point in Reconnect's flow, a deliberate, confirmed reversal of the "collapsed by default" decision logged 2026-08-11 (`01-decision-log.md`, "OOO/status-before-Transition sequencing") — Going Quiet's own OOO section was explicitly left untouched pending the open parity question logged below. **Real regression, not Reconnect-specific despite how it was reported:** `AdaptiveCircleChip`'s sent-fill (`chipSent`) was using the exact same background colour as an ordinary unselected chip, not the documented dark-green/white treatment — since this is the one shared chip component Going Quiet, Reconnect, and Taking Time's update all use, every sent chip in the app was affected, not only the one surface it was noticed on. Fixed at the shared component. Home's own leftover count (flagged, not fixed, in the entry two rows above) is now actually removed, on both the "Continue reconnecting" and "Finish Reconnecting" states. A hand-written "Close Circle" naming regression (missed by the 2026-08-10 rename) found and fixed in the safeguarding banner's alert/confirmation copy; an app-wide grep found no other user-facing instance. **Conversations (Library) rebuilt, after investigating first as instructed:** its existing per-Circle-card mechanic (an embedded shared box, a member list with per-person include/exclude, and its own separate "quick message vs. Personalise" swap) was confirmed to predate and diverge from the shared `PersonaliseAccordion` work built the day before, rather than reusing it. Rebuilt around Going Quiet's own dropdown-arrow convention: each Circle chip gets an independent arrow that adds its people into **one shared pill row** beneath the whole chip row (not a pop-up per Circle — corrected mid-pass from an initial per-circle-row draft), ordered to match each person's parent Circle's own left-to-right position; tapping a pill always opens the same `PersonaliseAccordion`, sent-state shown per person (not per Circle, a deliberate difference reflecting Conversations' own checklist-not-broadcast framing), never locked — a sent pill reselects to send again, same rule as everywhere else this pattern exists. "+" added, first in the row (maps to the existing add-ungrouped-person contact picker, replacing the old full-width button); "+" and "All" render bold. **Two capabilities not carried forward, flagged rather than silently dropped:** the old per-Circle "send one shared quick message to everyone at once" action has no equivalent in the new per-person model; the old manual "mark complete without sending" checkbox has no home in the new pill layout for Circle members specifically (still present, unchanged, for ungrouped people, whose own cards this rebuild left untouched). **Investigated, not built — a claimed three-tab (Conversations/Templates/Research) structure for this screen has no basis on record:** this book's own "Library screen structure" section documents exactly two things (Conversations primary, Templates secondary section, one continuous screen) with no mention of Research anywhere in Library; Research is, and always has been in this book's record, a separate Settings destination. Logged as an open question rather than built against an unconfirmed premise. Full technical detail lives in `hold-app`'s own `docs/09-decision-log.md`, not duplicated here | Every item traces to a specific bug report investigated to its real root cause (the chip-fill regression turned out to be app-wide, not screen-specific; the "Close Circle" regression needed a full audit, not a single-site guess) or a section of direct instruction, including two corrections issued mid-pass to the Conversations pill-row spec that were built to, not the earlier draft | Active — supersedes the per-circle-card description of Library/Conversations in the "Personalise pattern" section of `04-ux-content/04-navigation-architecture.md` (now updated in place, split between Circle-grouped and ungrouped people); supersedes the "collapsed by default" OOO framing for Reconnect's own post-coverage-complete moment specifically (Going Quiet's own framing is untouched); resolves and removes the "Home widget still shows a count" open question (fixed, not just flagged); two new open questions logged — Going Quiet/Reconnect completion-pattern parity, and Library's claimed tab structure |
| 2026-08-11 | Logged content from an external session write-up (design/decision-layer chat discussion, nothing built) across four files. **`08-decisions/04-open-questions.md`:** added "From your circle" (Hold+, future, deferred until after MVP + Phase 10 paywall — sender-initiated circle contributions via time-limited link, v1 template-first scope, later-phase sticker/widget-photo scope, resolved content-never-blind/styling-can-surprise preview model, 6 open sub-questions preserved as open); added "Notification content ideas" (historical/cultural facts, quotes with attribution risk, per-category toggle need — consolidated into one entry per the source write-up's own recommendation); updated the existing "Safeguarding trigger logic and wording" entry with reviewer-hiring research (CSO/clinical psychologist role definitions, confirmation that no public ready-made trigger-phrase list exists, Samaritans' guidance being weighted toward public content moderation rather than Hold's private 1-to-1 problem) — **flagged that `safeguarding-logic-framework-DRAFT.md`, referenced as built in that session, does not exist in either `hold-book` or `hold-app`**, checked directly, not found. **`07-business/02-pricing-principles.md`:** added a new flagged section recording a direct, same-day conflict — the write-up's "3-month as trial, converts to monthly-or-yearly" direction contradicts this document's own already-resolved "£4.99/3-month permanent parallel tier, no trial, no monthly cadence" (2026-08-11 correction, earlier the same day) — both recorded, neither silently chosen. **`06-privacy-security/04-content-retention.md`:** added a lapsed/dormant Hold+ backup-account retention proposal (30-day grace period, 6–12 month dormant window, mandatory pre-deletion warning, push-notification delivery with a flagged "app deleted, unreachable" gap) — explicitly scoped to the opt-in backup feature only, not free-tier local data, and explicitly not yet legally/clinically confirmed. **`07-business/06-business-strategy.md`:** elaborated the existing diagnosis-agnostic positioning note with a concrete "episodic capacity drop" audience-segment list (burnout, episodic depression, acute grief, Long Covid/ME/CFS/autoimmune flares, postpartum, neurodivergent burnout, acute crisis) — consistent with, not contradicting, the existing positioning. **Deliberately not logged:** the write-up's comparator-app growth timeline, launch-date estimate, and MVP build-status snapshot — all explicitly self-described in the source write-up as a day-old, unverified snapshot superseded by a live status check already requested elsewhere, so logging it as permanent book content would have enshrined stale planning data | Direct instruction to log a large external write-up; several items required flagging real conflicts or gaps (the pricing direction, the missing framework file) rather than silently reconciling or assuming completion, consistent with how every other conflict has been handled this session | Active — the pricing conflict and the missing safeguarding-framework file are the two items here that need a human decision/action before they can be closed, not just documentation; everything else is logged as future-spec or proposal, none of it built |
| 2026-08-12 | Distributed a fuller version of the 2026-08-11 session write-up across the book properly, per direct instruction, rather than leaving it as a standalone file — this pass both refined placement from the prior day's version and resolved the two items it had left open. **Pricing conflict resolved, not silently:** the 3-month tier is confirmed final as a paid introductory trial converting to monthly-or-yearly, not the earlier same-day "permanent tier, no trial" entry — `07-business/02-pricing-principles.md` updated with the earlier entry struck through in place (**superseded, not wrong** — a legitimate earlier decision point in the same day's thinking, per direct instruction not to characterise it as a mistake), the new resolution added as its own section, and the still-unspecified ongoing monthly price flagged as a real gap, not filled in. Added a cross-reference pointer (not a duplicate) to `07-business/06-business-strategy.md`. **Safeguarding framework file created:** `06-privacy-security/05-safeguarding-logic-framework-DRAFT.md` committed to this repo — previously referenced but confirmed missing, that gap is now closed. **Safeguarding research integrated into the primary architecture doc**, not just the open-questions tracker: `06-privacy-security/03-safeguarding.md` gained sections on finding a clinical reviewer (CSO/clinical psychologist roles), why no ready-made trigger list exists (NICE, StayingSafe.net, Samaritans Online Excellence Programme and its public-content-moderation-vs-private-drafting limitation), a pointer to the draft framework, an international crisis-resource table (confirmed core six: UK/Ireland/US/Canada/Australia/NZ, several more markets explicitly flagged as researched-but-unreliable rather than guessed at), and the decided onboarding region/language-detection approach; the corresponding `08-decisions/04-open-questions.md` entry was trimmed to a pointer rather than left duplicating the fuller content. **Audience reframing promoted to its own labelled subsection** in `07-business/06-business-strategy.md` (was an embedded note). **Data retention placement flagged back explicitly:** the instruction named `03-privacy-model.md`, which doesn't exist in `hold-book` (it's a `hold-app` doc); kept the existing `06-privacy-security/04-content-retention.md` placement from the prior pass and said so plainly rather than assuming it was uncontroversial, with its three open sub-questions also pointed to from `04-open-questions.md` directly, per instruction. **Live MVP status check run against the actual `hold-app` codebase**, superseding the stale write-up snapshot in `07-business/06-business-strategy.md`: confirmed core journey/Circles/AI-infra built and under active iteration; confirmed the Settings drawer has only 5 of 12 rows functional (Notifications/Language/Connected Accounts/Terms are explicit "Coming later" placeholders in code) and directly contradicted the snapshot's claim of built Accessibility/Personalise/Language rows — neither Accessibility nor Personalise exists as a settings row at all; confirmed Phases 4 (AI quality polish), 6 (accessibility audit), 7 (widget/moon-cycle), and 10 (monetisation/IAP) are entirely unstarted by direct code search (no matching files, components, or dependencies found); Phase 8 status matches what was already verified directly earlier this session (transition screens live, unchanged copy, no citation markers) | Direct instruction to distribute properly rather than leave standalone, resolve the two previously-flagged items, and replace an admittedly-stale snapshot with a real check rather than a repeated one | Active — this pass is complete pending the user's own diff review, which was explicitly requested before anything is considered final; files touched: `07-business/02-pricing-principles.md`, `07-business/06-business-strategy.md`, `06-privacy-security/03-safeguarding.md`, `06-privacy-security/04-content-retention.md` (unchanged from the prior pass, placement confirmed), `06-privacy-security/05-safeguarding-logic-framework-DRAFT.md` (new file), `08-decisions/04-open-questions.md` |
| 2026-08-12 | Reconciled the Settings drawer's documentation against a live code check run in a separate session, per direct instruction, across three items. (1) **Build-status record corrected in `04-ux-content/04-navigation-architecture.md`'s "Settings panel structure":** every row now marked Built or Not built, matching `hold-app`'s actual `SettingsDrawer.tsx` — Notifications, Language, Connected Accounts and Terms are explicit `ComingLaterRow` stubs in code, and no Accessibility or Personalise row exists at all, contradicting an earlier session write-up's optimistic claim (already corrected in `07-business/06-business-strategy.md`'s MVP status check the prior day; this brings the primary UX-spec doc into the same state, since it previously listed rows with no built/not-built distinction at all). (2) **New target design logged: a single merged "Accessibility & Display" row**, superseding the earlier separate-Accessibility/separate-Personalise idea implied by that same write-up (never built, not a formal prior decision row in this log — the only trace of it was the write-up's own claim, now corrected). Two sub-groups specified: Reading (text size, a four-option font choice — System default, Lexend, Atkinson Hyperlegible, OpenDyslexic, with Verdana/Arial/Open Sans explicitly considered and cut as redundant with System default — and an in-app Reduce Motion override) and Look & Feel (display theme, a warmth bar applied as a relative offset on top of Hold's existing per-flow-state warmth shifts rather than an independent palette override, Light/Dark/System, and a moon-phase toggle). **Flagged, not confirmed:** WCAG contrast compliance across the full warmth-bar range still needs verifying before this ships. Explicitly logged as target design, not built — confirmed via the same live code check. (3) **Hold+'s persistent top-bar element reversal — verified already correctly documented, no content fix needed.** Checked `04-ux-content/04-navigation-architecture.md`'s "Hold+ visibility" section and this log's own 2026-07-29 entry ("Dropped Hold+'s persistent top-bar element entirely, superseding the earlier two-access-point wording") — both already state the removal correctly; nothing was silently left showing the old model. Included here per instruction to summarise all three items together, not because a correction was needed on this one | Direct instruction following a live status check that found the drawer's documented state materially ahead of its actual code; item (3) required verification rather than a fix, and is reported as such rather than padded out as if a change had been made | Active — `04-ux-content/04-navigation-architecture.md` and `07-business/06-business-strategy.md` now agree on Settings-drawer build status; the Accessibility & Display page is a confirmed target design, not yet built; the warmth-bar contrast question remains open |
| 2026-08-13 | **Two real audience-persistence bugs fixed (a duplicate-key/cross-contamination bug from Circle reassignment, and a separate one from plain exclusion never reaching the persisted record — the second contradicts this book's own "frozen at send time" intent); Reconnect's bundled-Circle handling rebuilt around an always-real Circle with an optional rename, not a yes/no permanence gate; "+"/"All" pinning confirmed app-wide; Reconnect's separate Send button removed; Reconnect gets its own real per-Circle templates; a new composition-driven nav-bar/swipe-back rule added.** On-device testing surfaced a circle-selection bug in Reconnect (selecting one Circle also selected an unrelated one) traced to `buildAudienceCircles` never actually removing a reassigned contact from their original Circle's persisted record, only from the session-local send logic — fixed at the source, which also resolved an accompanying React duplicate-key error. A second, related gap found investigating the first: a contact simply excluded at Going Quiet (not reassigned anywhere) also stayed in the persisted audience regardless, contradicting this book's own documented "frozen onto the HoldPeriod record at send time" intent — fixed the same way. Reconnect's ad-hoc bundled Circles no longer go through a yes/no "should this exist" prompt — they're made real immediately, with an auto-generated initials placeholder name ("P & A"), and Reconnect now offers only an optional, always-final rename (next to Hold's own circle glyph), sequenced after sends and before the Personalise choice, appearing regardless of whether that Circle was actually messaged. "+"/"All" pinned-outside/first-inside-scroll is now confirmed as an explicit app-wide convention, not a Going-Quiet-specific placement, applied to both of Reconnect's rows and fixed in Library where it had drifted. Reconnect's separate always-available Send button is removed — the docked bar's own send icon is the only way to send there now, matching a "one consistent mechanism" instruction that was explicitly *not* extended to Going Quiet's identical button (logged as its own open question). Reconnect gained real per-Circle/combination saved templates (separate storage from Going Quiet's own, since the content is a different kind of message — short and instant, not a full redraft), replacing a hardcoded quick-message list. Separately: the bottom nav bar now hides, and swipe-back disables, during any active text composition anywhere in the app, as one shared mechanism rather than a per-screen list — investigated first and found no existing version of this to extend, despite the request assuming one existed. Full technical detail lives in `hold-app`'s own `docs/09-decision-log.md`, not duplicated here | Every item traces to a specific on-device bug report investigated to its actual root cause (both audience bugs), a direct instruction with its own confirmed answers (the Reconnect redesign items), or an explicit build request investigated against the current codebase before starting (the nav rule) | Active — supersedes the yes/no Circle-permanence-gate description of Reconnect's own section in `04-ux-content/01-core-journeys.md` (now rewritten) and the Reconnect Send-button/template framing in the same section; new "Bottom nav visibility during active composition" section in `04-ux-content/04-navigation-architecture.md`; "+"/"All" pinning convention in the same file marked explicitly app-wide; two new open questions logged (Going Quiet's own Send button; nothing else) |
| 2026-08-13 | **Bottom nav bar's flat, hardcoded-palette background box replaced with a theme-aware frosted-glass pill; a real dead-end navigation bug fixed in Post-Reconnect.** On-device bug report: the nav bar appeared to only show on Home, and a solid box was visible behind the pill on every screen. Investigation found the "hidden elsewhere" half was expected/on-record behaviour (Going Quiet and Reconnect are stack screens outside the tab group, never had a nav bar to begin with — see the entry above), not a new bug — but the box was real: `BottomTabBar.tsx`'s wrapper painted a flat, hardcoded-to-normal-palette fill behind the pill regardless of what screen sat behind it, including visibly clashing with Home's own quiet-palette background during an active Hold. Fixed: the wrapper paints no background at all now; the pill itself became a blurred, translucent surface (`expo-blur`) tinted to whichever palette Home is currently resting in, via a new shared `QuietPaletteContext` that Home writes to and `BottomTabBar` reads (`BottomTabBar` sits alongside the tab screens, not inside Home's own tree, so it had no way to know Home's palette state otherwise). Separately, found and fixed a real dead-end: Post-Reconnect's "Finish Reconnecting" action routed to plain `/library` with no way back into Reconnect's own remaining steps (OOO/status, the bundled-Circle naming opportunity, Done) — now routes to `/return/reconnect`, matching the sibling "Continue reconnecting" case, which already did this correctly | On-device bug report (nav bar visibility/styling) investigated to its actual root cause, separated from the already-correct-and-on-record structural absence on Going Quiet/Reconnect; the Post-Reconnect dead-end was a direct instruction, confirmed as a real bug (not a naming choice) by tracing the route it dead-ended into | Active — "Bottom nav visibility during active composition" section in `04-ux-content/04-navigation-architecture.md` gains a new "Bottom nav bar surface" subsection describing the frosted/theme-aware pill in place of the flat box; full technical detail in `hold-app`'s own `docs/09-decision-log.md` |
| 2026-08-14 | **Going Quiet's per-Circle member dropdown brought onto the app-wide pill-row convention, replacing its own older vertical selection-circle list.** A consistency sweep across the app's four "reveal a Circle's people on demand" screens found three already on the same `AdaptiveCircleChip`-in-a-horizontal-`ScrollView` pattern (Reconnect, Library) or a structurally different case (Your Circles' include/exclude editor, a toggle UI rather than a viewer, left as-is) — Going Quiet's own `RecipientPersonalisation` component was the one outlier, still rendering a vertical list of plain rows with a separate circular "+" button and a `SelectionCircle` per person. Rebuilt to match: pinned "+" chip beside a horizontal pill row, one `AdaptiveCircleChip` per included person, tap to exclude (same removal behaviour as before — excluding moves them to the removed-people roster, unchanged). The single-contact exception is preserved: a Circle with only one member still shows no removable control, just their plain name beside the "+" | Direct instruction, following a same-session finding that the pill-row convention was already established elsewhere and Going Quiet was the gap, not a case of building the pattern for the first time | Active — `04-ux-content/01-core-journeys.md` point 2's per-person description rewritten from "selection circle" to pill-row language; `hold-app`'s `RecipientPersonalisation.tsx` rebuilt accordingly, `tsc --noEmit` and `vitest run` (41/41) both pass |
| 2026-08-19 | **No OS-level, system-wide "Hold Mode" toggle is achievable through Apple's Focus APIs; native SMS/Mail confirmed to require a user-visible send every time; SMS has no BCC equivalent.** Focus is local to the user's device and not broadcastable to other people or apps — the Focus Status API and Focus Filter API let Hold *detect and respond to* an active Focus (e.g. suggest Going Quiet when a Rest/Sleep Focus turns on), but cannot create a new Focus-like mode visible system-wide or to other people; any "Hold Mode" concept stays app-internal. Per Apple DTS staff answering directly in the Apple Developer Forums, silent/background sending via `MFMessageComposeViewController` (SMS) and `MFMailComposeViewController` (Mail) is explicitly not supported — no workaround short of Hold running its own backend relay (see the backend-relay row below for why that's rejected). A narrow exception, Apple's Critical Messaging API (emergency check-in apps, special entitlement, background-only), doesn't fit Hold's use case — noted so it isn't mistakenly revisited later as a solution. SMS itself has no BCC equivalent: `MFMessageComposeViewController` either creates one shared group thread across every recipient added to a single compose call, or requires separate individual sends — there is no way to send one SMS that multiple recipients receive without seeing each other or each other's replies. **New detail, surfacing an open question:** the composer does support adding multiple recipients to one compose call, resolved with a single confirmation tap rather than one per recipient — this reduces confirmation count but reintroduces the shared-thread problem, so it isn't a free win; see `04-open-questions.md`. **Reaffirmed, not new — and closed, not deferred, per the same wording convention as "Hold as default SMS handler" and the text-to-speech voicemail greeting (`04-open-questions.md`):** cross-platform auto-sync of status to external social platforms (Instagram, WhatsApp, etc.) is blocked by lack of third-party write APIs for most platforms — the existing Wider World clipboard/deep-link pattern (`04-ux-content/01-core-journeys.md`) remains the realistic mechanism. Do not revisit without a materially different technical landscape (e.g. a platform policy change) — this was rejected on the merits, not deferred for later | Direct platform-capability research, confirmed against Apple DTS forum answers rather than assumed | Active — no change to any built feature; rules out "Hold Mode" and silent SMS/Mail send as future possibilities so they aren't re-proposed without this context; the multi-recipient-single-confirm detail is logged as a new, unresolved tradeoff in the open questions file, not decided here |
| 2026-08-19 | **Backend-relay silent send — considered, deliberately not pursued.** The only way to achieve genuinely silent, one-tap-to-everyone sending with no native compose UI at all: Hold's own backend (e.g. Twilio/SendGrid) sends on the user's behalf instead of through Apple/Android's native composer. Rejected for now on two grounds: the message would arrive from a Hold-owned number/address rather than the user's own, changing how it reads to the recipient (less "my friend sent me this," more "this is from an app" — working against Hold's premise of a message recognisably from someone the recipient knows); and it adds real per-message cost plus phone/email verification overhead | Direct discussion, weighed against the SMS/Mail platform constraints confirmed in the row above | Closed, not open — logged so it isn't silently re-proposed later without this tradeoff attached, same treatment as the Android-SMS-handler rejection (2026-08-11 row) and the TTS-voicemail/calendar-auto-blocking future-spec items in `04-open-questions.md` |
| 2026-08-19 | **Reconnect History enrichment: what to add, not yet where it hooks in.** Reconnect currently writes to Quiet History only at the period level (recipient names, start/end timestamps — see `docs/03-privacy-model.md`, `hold-app`). Decision: enrich the existing period-level entry, not replace it with per-message logging — add Taking Time duration (start of Taking Time to start of Reconnect) and which Reconnect steps were completed (Quick/instant message sent, Personalise/Thoughtful reply completed, or where the flow stopped if not finished). **Stays ONE entry per Hold period, deliberately, not one entry per message** — Reconnect is one journey arc, Conversations is per-relationship/personalised, and the two shouldn't later be "fixed" into consistency by mistake; this distinction is intentional, not an oversight to reconcile. Exact fields and hook-in points (`reconnect.tsx`, `return/instant.tsx`, `holdHistoryService.ts`/`holdHistoryFormat.ts` in `hold-app`) still need investigation and a plan before building | Direct instruction, decisions settled, implementation deliberately not yet started pending investigation | Active as a product decision; implementation plan pending, to be reported back before any code changes |
| 2026-08-19 | **Content richness staged by journey point, Taking Time vs. Reconnect — proposed, not yet confirmed as final.** Taking Time notifications/copy stay validation-and-permission only, matching what's already built ("You don't need to earn rest" register); fuller opt-in content (story, poetry, cultural/research material) becomes available from Reconnect onward via a toggle on the Transition screen (screen identity to be confirmed against the codebase before building, not assumed). **Reasoning:** Taking Time is the one point in the journey where the person has explicitly signalled "I need space" — even gentle opt-in content there risks working against that signal; Reconnect is different because the person has already chosen to move toward re-engagement, so richer content fits the moment rather than pulling against it | Proposed reasoning from a design-session discussion, not yet explicitly signed off as final | **Proposed, pending confirmation — treat as a strong candidate decision, not settled.** Also flagged in `04-open-questions.md` per the same status |
| 2026-08-19 | **Three content/copy decisions confirmed, each with reasoning recorded so it isn't re-litigated.** (1) **Reassurance/research/cultural/poetry content routes to Library → Research as an opt-in destination, not inline on Going Quiet/Reconnect/Conversations flow screens**, which stay at existing Transition-screen brevity — reasoning is evidence-based, not just a style preference: `02-research/06-guilt-spiral-and-supportive-language.md` already states that short, genuine statements that validate are more supportive than lengthy reassurances, which is the actual basis for keeping flow-screen copy brief and routing longer material to an opt-in destination instead. (2) **"You're reconnected" copy (Conversations' queue-complete redirect to `/return/done`) confirmed correct even for a single friend/single message, and for standalone use outside a Hold period** — an earlier concern about needing different copy for these cases was raised and resolved: the felt experience of reconnecting is present whether or not a formal Hold period was open, since the copy tracks the real thing that happened (distance, then reaching back out), not a technical state, so a Hold-period-tied vs. standalone copy split isn't actually needed. (3) **Permission-to-stop line for Conversations backlog-clearing, not encouragement-to-continue** — after every send (not count-gated, no "3 more to go"), a brief line like "You can stop here. What's sent is enough for today," same short register as existing Transition/Done screens; explicitly NOT "great, one more!" or similar — reasoning: even soft encouragement edges toward the praise/pressure pattern the guilt-spiral research explicitly warns against, which states never to praise the person for a basic act of communication | Each traces to either existing research already in the repo (1, 3) or a previously-raised concern now explicitly resolved (2) | Active — (1) and (2) require no further work, already-correct/already-explained; (3) is a new build item, not yet implemented; one narrow remaining edge case on (2) — Care/Professional contacts with no real prior distance to reconnect from — is unresolved, logged in `04-open-questions.md` |
| 2026-08-19 | **Acknowledgment for out-of-app contact (e.g. a logged call or voice note): no congrats/praise message.** Congrats/praise language was rejected because it frames a basic act of communication as an achievement, conflicting with the existing guilt-spiral research (never praise for basic communication) and risking a patronising tone. If any acknowledgment is shown at all, it's quiet and non-evaluative ("Noted" register, not "Well done!"), same tone family as existing Transition/Done screens. **The primary justification for this feature existing at all is Patterns/History accuracy** — so someone who reconnects by phone isn't invisible in their own Patterns data, undercounting their actual reconnection — not the acknowledgment itself; recorded explicitly so the feature isn't later justified or expanded on the "reward" framing instead. **Platform-constraints reasoning for why logging must be manual, not automatic:** iOS has no third-party API for reading call logs or message content at all, full stop, no workaround; Android restricts `CALL_LOG` and notification-listener access, via Google Play policy, to default phone/messaging handler apps only — the same default-handler-status barrier already investigated and rejected for SMS auto-reply (2026-08-11 row, "Hold becoming Android's default SMS handler... rejected"). Manual logging (native share sheet, 2026-07 row) remains the only realistic mechanism on either platform | Direct instruction; the "why manual" reasoning traces to the same platform-policy research already used to reject default-SMS-handler status elsewhere | Active decision (no congrats, quiet-if-any acknowledgment, Patterns-accuracy justification); not yet built |
| 2026-08-19 | **Eight on-device findings on the pill-row rebuild worked through; the priority one (Box A not opening Box B on tap) investigated first and traced to a real regression, not a new/separate break.** The 2026-08-13 fix that made Box A's closed preview scroll internally had removed its whole-box tap-to-open in favour of a small "Edit" link — reasoned at the time to be necessary to avoid a Pressable/ScrollView gesture conflict, but never actually confirmed on-device, and Going Quiet's own message pre-fills (saved default or intent-chip draft) almost immediately in real use, so the "Edit"-only state was effectively the *only* state anyone ever saw — tapping the box read as fully broken. Fixed by restoring one whole-box `Pressable` for both empty and filled states (matching this component's own stated "same interaction everywhere" design goal) and using RN's actual documented mechanism for a nested ScrollView (`nestedScrollEnabled`, Android-specific) instead of removing the Pressable — evidence for the original diagnosis being wrong: removing the Pressable never fixed scrolling either, confirmed still broken in the same on-device report. **Confirmed as a genuinely separate issue from the post-send lockout** (a different, never-addressed code path with no edit affordance built at all), not the same root cause. Circle dropdown-vs-selection independence, previously stated but not actually true in code, is now fixed (an unselected Circle's arrow silently did nothing; now shows a read-only member preview). Recipient pills and their "+" resized to a new small content-hugging `compact` chip variant, applied consistently to every person-pill row app-wide (Going Quiet, Reconnect, Library), not just Going Quiet's own — top-level Circle-picker rows untouched. Emoji now stripped from contact names wherever a pill renders one. "Save to Library" shortened to "Save," moved to the right of Template. Send's position (proposed before building, per instruction not to default to three equal-weight buttons): confirmed as Template — Send — Save in one row, Send kept as the app's one standard filled/primary send button for visual distinction rather than a new treatment | Every item traces to a specific on-device bug report investigated to its actual root cause, or (Send's layout) a direct question confirmed before implementing | Active — `04-ux-content/01-core-journeys.md` points 2 and 4 updated (dropdown independence + read-only preview; Template/Send/Save layout and "Save" wording); full technical detail in `hold-app`'s own `docs/09-decision-log.md`, not duplicated here |
| 2026-08-19 | **In-app "on Hold" presence indicator (seeing when another Hold user is currently on Hold) — assessed, not rejected.** Technically possible (contact-matching against a server, similar in kind to how Signal/WhatsApp do contact discovery, though a genuinely privacy-respecting version is non-trivial) but philosophically in tension with Hold's own privacy/non-pressure principles: a persistent, ambient status is a different category from something explicitly shared with a specific recipient in a specific moment, and it would reintroduce the exact "why haven't they replied" checking-in anxiety Hold exists to remove, just displaced onto the person watching. **Conclusion: should never be ambient or discoverable — only ever visible to someone explicitly included in a Going Quiet Circle, and only if this is picked up in future.** **This is NOT a closed/rejected feature, unlike TTS voicemail generation or the Android-SMS-handler item** — it's an open, constrained idea: if it resurfaces, revisit only with that visibility rule intact, don't build an ambient/discoverable version even if requested later without checking this note first. **Distinct from, and not to be merged with,** the automatic cross-platform status-syncing item (Instagram/WhatsApp/etc. auto-sync, blocked by lack of third-party write APIs — logged in this file's earlier 2026-08-19 entry) — different features, different constraints | From a separate design session (2026-08-17) not previously written into hold-book; direct instruction to record it now, distinct from the platform-API-blocked non-feature it's easily confused with | Open, constrained — see `04-open-questions.md` for the summary pointer; not built, not rejected |
| 2026-08-20 | **Excluded-recipient visibility built for both Going Quiet and Reconnect; Reconnect's "+" becomes a real Circle with its own apology wording; Trigger 2/3 completion tracking split into two genuinely separate mechanisms, each with its own gating rule settled after considering and rejecting a fuzzier alternative.** Full technical detail lives in `hold-app`'s own `docs/09-decision-log.md`; summarised here since several corrections landed mid-build, worth having on record in one place. **Trigger 2 ("I've already told them") vs. Trigger 3 ("Conversation complete") confirmed as two distinct mechanisms, not one merged concept** — Trigger 2 lives in Reconnect only, satisfies the same completion gate a real send does, and now writes to Hold History (new, wasn't originally meant to persist); Trigger 3 lives in Conversations only, is a standalone completion log covering replied-in-Hold/elsewhere/phone/in-person/no-further-reply-needed as one mark, gates nothing, and only attaches to Hold History while inside an active Continue-reconnecting session for that period — **a "most recent matching period" lookup was proposed, then explicitly rejected**: outside an active session, a Conversations completion is unattached, full stop, no guessing. **Trigger 2's own placement was corrected twice before landing**: first a two-option prompt at unselect (rejected — adds friction to every single unselect); then automatic Circle-of-one spin-out on unselect, matching a mechanic that turned out not to exist in Reconnect's actual code at all (`togglePersonIncluded` is a plain reversible toggle, verified directly rather than assumed); settled as Reconnect's own new passive excluded line (mirroring Going Quiet's, built fresh since Reconnect had no prior bundling mechanic to extend), with "I've already told them" and a "+" bundle-into-Circle action both available on it, keyed by phone number not name. **Reconnect's "+" now creates a real provisional Circle-of-one**, closing a previously-flagged 2026-08-13 inconsistency (it used to add an ungrouped contact, the one place in the app not following the confirmed Circle-of-one convention) — that Circle gets a double-line border and its own dedicated apology-wording suggestion pill ("Sorry I wasn't able to let you know, I've been taking some quiet time."), available (never default) wherever that state can be messaged, Reconnect and Conversations both. **Two pre-existing colour-only accessibility violations found and fixed while building the above, per the standing rule that colour alone is never a sufficient state distinction** — Going Quiet's "claimed" roster marker and Conversations' checkbox fill both gained a "✓"/shape marker alongside their existing colour change | Every correction traces to either a direct instruction reversing an earlier plan, or a claim verified against the actual code before being built against (the non-existent auto-spin mechanic; the "most recent period" lookup considered and rejected in favour of a stricter rule) | Active — `04-ux-content/01-core-journeys.md`'s Going Quiet point 2 and Reconnect points 1-2 (renumbered, cross-reference at "point 6" updated accordingly) updated in place |
| 2026-08-20 | **Library and Reconnect's Conversations screens unified into one implementation — direct instruction, choosing the bigger refactor over a smaller data-only patch.** Verified first, before deciding scope: "Continue Reconnecting" routes straight to `/return/reconnect`, never through Library — the two screens were two separate implementations both reading the same `conversationService` store (same data, different query scope, different rendering), sharing only the `PersonaliseAccordion` component itself. Confirmed as a real user-trust problem, not just an inconsistency: someone relying on Hold to keep things organised at low capacity could see a different "what's pending" picture depending on entry point. New `useConversations` (state/query/actions) + `ConversationsView` (rendering) replace the old `usePersonaliseCompletion`/`PersonaliseCandidateList` pair (deleted, zero remaining importers confirmed first) — one shared implementation, parameterised by scope (Library: unscoped, everyone; Reconnect: scoped to just that period's own audience) and render mode (Library: circle-browsing/selection chrome; Reconnect: flat, since its audience is already fixed). Built in three verified phases (extract → re-wire Library alone → re-wire Reconnect, merging its own docked-bar priority chain with the shared hook's), each checked with `tsc`/`vitest` before moving to the next. **Real, intended consequence, not a side effect:** Reconnect's post-coverage-complete step is now the full Library-equivalent experience (per-person card, checkbox, Quick-message/Personalise swap) instead of a flat accordion list, gaining the permission-to-stop line and "Conversation complete" inside Reconnect's own flow for the first time — exactly the "same component, same actions, regardless of entry point" the unification was for. **Explicitly not verified on-device** (no simulator/device access this session) — flagged, not treated as covered by a clean typecheck, given docked-bar/input-state issues have been a real source of bugs in this project before. `04-ux-content/04-navigation-architecture.md`'s "Personalise pattern" section, which named the old hook/component explicitly, corrected in place | Direct instruction, choosing the larger refactor after the smaller option was explicitly offered and declined, given the trust cost of two diverging "what's pending" pictures | Active — implementation complete and verified at the code level; on-device confirmation in both contexts (standalone Library, embedded in Reconnect) still needed before this is considered fully settled |
| 2026-08-12, reaffirmed 2026-08-19 | **App icon customisation/theming — considered and explicitly rejected.** Letting a user choose or theme Hold's home-screen app icon | Adds choice without improving usability for someone at low capacity; delight without a usability benefit fails the "does this make the flow more usable" test already applied to Personalise-type additions elsewhere in this project. Reaffirmed 2026-08-19: cut as decorative, having failed the low-capacity usability test a second time on review, not just carried forward unexamined. Note: `hold-app/docs/07-release-checklist.md`'s "App icons and splash assets approved" line is an unrelated release-asset checklist item (confirming the store-listing icon itself is ready), not coverage of this rejected feature — the two should not be conflated | Active — closed, philosophy; see `08-decisions/02-ideas-not-to-build.md` |
| 2026-08-13 | **Rejected a horoscope-style daily habit-loop notification.** A recurring notification designed to be opened and checked daily regardless of whether anything was actually needed | Combines two patterns already ruled out elsewhere in this project rather than being a new problem: it's a habit-loop engagement notification (horoscope-style, designed to be checked daily on a schedule, not in response to a real need), and it names the guilt spiral back at the user as a persuasion technique. Distinct from the separate daily-tick-vs-weekly-rest-day cadence question for a future tracking feature, logged 2026-08-19 in `08-decisions/04-open-questions.md` — that's an open question about *how* an opt-in feature might work, this is a closed rejection of a specific *notification pattern*; the two should not be merged | Active — closed, philosophy; see `08-decisions/02-ideas-not-to-build.md` |
| 2026-08-19 | **Do Not Disturb / Focus as a substitute for a voicemail greeting — considered and rejected.** Whether turning on DND/Focus for incoming calls during Taking Time could stand in for the still-unbuilt text-to-speech voicemail-greeting idea (`08-decisions/04-open-questions.md`) | Rejected as insufficient: DND/Focus silences incoming calls but carries no message to the caller, so it doesn't actually communicate anything — it solves "don't make the phone ring" but not "tell the caller what's going on," which is the actual point of a voicemail greeting. Distinct from, and not to be merged with, the same day's separate "Hold Mode" ruling above, which concerns whether Focus state can be broadcast system-wide to other people/apps — this is narrower: DND/Focus specifically as a stand-in for a voicemail greeting's content | Active — closed, platform |
| 2026-08-19 | **Android Notification Listener Service access — ruled out.** Whether Hold could use Android's `NotificationListenerService` to read other apps' incoming notifications | The only Android path to reading other apps' notifications at all — so privacy-invasive and so heavily scrutinised under Google Play policy (a sensitive-permission declaration, case-by-case review, has been used to reject or pull unrelated apps) that it's effectively ruled out as a design choice rather than left open as a future possibility. Pairs with the existing iOS finding logged elsewhere the same day (no third-party API exists for reading call logs or notification/message content at all, full stop): impossible on iOS, and ruled out on Android on privacy-scope grounds rather than merely deprioritised | Active — closed, platform |
| 2026-08-19 | **Share to Hold: manual share-extension only — confirmed as the only viable message-capture mechanism.** From the same research session as the Notification Listener Service entry immediately above, investigating how another app's message content could reach Hold for drafting help | iOS has no third-party API for reading another app's message content at all, full stop — no workaround exists, and this is the platform constraint that rules out any automatic-capture approach on iOS entirely. Android's Notification Listener Service path is separately ruled out on its own privacy/Play-policy grounds (see the entry immediately above) — a distinct constraint, not the same one restated. Given both, the only viable mechanism on either platform is the user manually invoking their own OS share sheet (iOS Share Extension / Android share intent) and choosing Hold as the target — Hold never sees a message's content unless the user actively shares it that way, no automatic capture under any circumstance. Consistent with the existing privacy-by-design principle that Hold only knows what's explicitly shared with it | Active — concept only, not built; a manual Share Extension/Android share-intent entry point would sit alongside Conversations' existing manual-paste entry point as a second, equally manual way in, not a replacement for it |
| 2026-08-19 | **Text-to-speech voicemail greeting reclassified: closed, not deferred — correcting the 2026-08-11 "deliberately scoped out of this pass" framing.** Direct instruction: "mark voicemail as not a future spec" | Same underlying platform constraint as before (no public API on either platform for a third-party app to set someone's actual carrier voicemail greeting) — what changed is the framing, not the technical finding. "Deferred" implied this might be picked up once some unspecified blocker clears; "closed" states plainly that it won't, absent a materially different technical landscape, so it isn't quietly re-proposed as a future Wider World channel. Same wording convention now applied to "Hold as default SMS handler" (`04-open-questions.md`) and automatic cross-platform status syncing (row above) — all three use "closed, not deferred," so deferred and impossible are never ambiguous again across platform-constrained items | Active — closed, platform; `04-open-questions.md`'s "Additional Wider World channels" entry corrected to remove this item from its future-spec list, with its own dedicated closed section added there |
| 2026-08-20 | **AI-inferred sender guessing — ruled out.** Whether AI could guess who a pasted "what they sent" message is from, when the user hasn't said | Listed among the ideas explicitly rejected alongside per-app counters and background access. Consistent with `06-privacy-security/02-ai-boundaries.md`'s existing "infer relationship facts not supplied by the user" rule — this makes explicit that the rule also covers guessing who a message is from, not just what it means. Per-app counters are already covered by `04-ux-content/04-navigation-architecture.md`'s "No badges, no counts" section; background access is covered by the Android default-SMS-handler and Notification Listener Service rejections above; sender guessing itself had not previously been written down anywhere in this book | Active — closed, philosophy; see `08-decisions/02-ideas-not-to-build.md` |
| 2026-08-21 | **Linked circles (Olympic-rings) extended from Taking Time's "Send an Update" to Going Quiet, Reconnect, and Conversations — same mechanic, a trigger it didn't previously have.** Going Quiet's own combined sends (two or more Circles sent one message together) now create a link, carried forward within the same Hold period into Reconnect's instant-message screen (grouped by default, same Group/Ungroup toggle) and Conversations (no per-Circle selection row of its own, so it reflects the choice as adjacency in its person list instead). Investigated before building, per instruction: the existing mechanic turned out to be built on Update's own private saved-template storage, not a portable, reusable fact — extracting the shared rendering (`LinkedCircleCluster`/`LinkGroupToggle`) was straightforward, but "linked" itself had to become its own concept | **Period-scoped, not permanent — a scope correction made mid-conversation.** Linking was first proposed as a standing relationship between Circles, independent of any one Hold period; corrected to match Taking Time's own actual design intent more closely (its own "ungroup" choice is already session-local, resets on reopen) — a link only lasts the current period, a new Going Quiet starts with none inherited, even for the same Circles combined-sent again later | Active — `04-ux-content/01-core-journeys.md`'s existing "Grouped/linked Circles" note (Taking Time-only) extended in place; full technical detail in `hold-app`'s own `docs/09-decision-log.md` |


